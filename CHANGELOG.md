# 更新日志

## v1.5.3 — 「打开所在文件夹」的专项压测

v1.5.2 修掉了「带空格的路径必错」那个主因，但「任意路径都可用」需要被证明而不是被相信。
这一版新增 `scripts/stress_open_folder.py`，分两层压：

| | 做什么 | 规模 | 结果 |
| --- | --- | ---: | ---: |
| A 层 | 用 Windows 自己的 `CommandLineToArgvW` 当裁判，验证 explorer 拿到的参数逐字等于目标路径 | **61,545 条** | 0 不一致 |
| B 层 | 真起资源管理器，用 `Shell.Application` 读回它**实际停在哪** | **34 条** | 0 失败 |

A 层语料含特殊字符、各语种、emoji、组合字符、双向控制符、长度扫描，以及 6 万条随机 fuzz（
字母表是 Windows 文件名里**一切合法字符**）。

### 压出来的真 bug：盘符根

61,545 条里只有 **5 条**往返失败，全是尾部反斜杠——也就是盘符根 `C:\` 和 UNC
共享根。`"C:\"` 里的 `\"` 会被当成**转义引号**，参数变成 `/select,C:"` 且引号不闭合。

顺这条线真机实测，得到三个反直觉的事实：

```
os.startfile('C:\')                窗口集合完全没变化——什么都没发生
ShellExecuteW(open/explore/None)   同上，三种动词都没反应
explorer.exe "C:\\"                落到「文档」
explorer.exe /select,C:\           落到「此电脑」
explorer.exe C:\                   ✅ 正确
os.startfile('C:\Windows')         ✅ 正确（普通目录是好使的）
```

1. `os.startfile` 对**普通目录**没问题，偏偏对盘符根毫无反应。所以不能想当然地拿它兜底。
2. **explorer 不按 `CommandLineToArgvW` 那套反转义**：`"C:\\"` 里的 `\\` 不会被还原
   成一个反斜杠，而是原样当成路径的一部分。所以此处**绝不能加引号**；盘符根固定是
   `X:\` 三个字符、不含空格，不加引号是安全的。
3. 同一个坑 `open_file` 也踩着：**双击一个「D:\」这样的文件夹结果，之前是没任何反应的**。

修法：`open_folder` / `open_file` 先把根路径分流给新的 `open_drive_root`（盘符根走不加
引号的 explorer，UNC 共享根走 `os.startfile`——它对 UNC 实测是好使的），这也顺带保证了
传给 `explorer_select_command` 的路径永不以反斜杠结尾。

### 一个确认**不是** bug 的边界：超 MAX_PATH

超过 260 字符的路径，`open_folder` 返回 False、UI 提示「找不到所在文件夹」。这是**对的**：
本机 `LongPathsEnabled=0`（Windows 默认值），资源管理器自己也定位不了——`/select` 裸路径、
直接打开目录、加 `\\?\` 前缀三种写法实测全部回落到「桌面」/「文档」。优雅失败比打开一个
错目录强。

### 其他已覆盖且全部通过的场景

特殊字符（空格 / 逗号 / `&` / `%` / `^` / `!` / `#` / `$` / 括号 / 引号 / 反引号 / 分号）、
中日韩俄希、变音与组合字符、emoji、全角空格、`%TEMP%` 样式的名字（不经 cmd，确认不展开）、
`~$` 开头的 Office 锁文件名、超长名、深层嵌套、目录本身作为目标、隐藏 / 只读文件、
junction、UNC 目录与共享根、文件已删但父目录在、父目录也不在，以及 40 次连点（零异常，
13.8 ms/次）。

`tests/test_open_folder_quoting.py` 从 15 条扩到 **31 条**，把上面每一条实测结论都钉成断言。

## v1.5.2 — 三个用户反馈

### 「打开文件夹」经常打开成别的文件夹

用户反馈定位文件时经常跳到不相干的目录。真机复现（Windows 11，用 `Shell.Application`
读回资源管理器实际停在哪）：

| 文件路径 | v1.5.1 打开了 | 应该打开 |
| --- | --- | --- |
| `…\PPT Doctor\demo.pptx` | **`C:\Users\<me>\Documents`** | `…\PPT Doctor` |
| `…\comma,dir\demo.pptx` | **`C:\Users\<me>\Desktop`** | `…\comma,dir` |
| `…\a & b\demo.pptx` | **`C:\Users\<me>\Documents`** | `…\a & b` |
| `…\plaindir\demo.pptx` | `…\plaindir` ✓ | `…\plaindir` |

根因是两条引号规则打架。`subprocess.Popen(["explorer", f"/select,{path}"])` 交给
`list2cmdline` 拼命令行，它见参数含空格就把**整个** `/select,路径` 包进引号：

```
explorer "/select,C:\Users\me\My Docs\PPT Doctor\a.pptx"
```

而 explorer 的解析器是非标准的，要求引号只包路径（`explorer /select,"…"`）。整体
加引号它认不出来，于是**默默回落到默认文件夹**——「文档」「桌面」这类位置看起来
就像"打开了个别的文件夹"。不加引号时，路径里的逗号又会被当成分隔符截断。

**只要路径含空格就必错**。开发机上随手一试往往是好的（`plaindir` 那一行），所以
这个 bug 能一直活到用户手里。

修法是把引号放对位置：`explorer.exe /select,"路径"`，且传字符串而不是列表
（列表就会重新落进 `list2cmdline` 的加引号规则），并走 `%SystemRoot%` 绝对路径
而不是 PATH。实测四种路径全部正确定位。

顺带量了一次**不该**怎么修：`SHOpenFolderAndSelectItems`（shell API）看着更
「正确」——完全不经过命令行解析——但它是同步的，需要新开资源管理器窗口时实测
阻塞 **1,465～1,742 ms**（复用已有窗口 42 ms）；而 explorer 在自己进程里干活，
`Popen` **30～55 ms** 就返回。health / report 那几个调用点是直接在 UI 线程上调
`open_folder` 的，换成同步版本等于把一个错误 bug 换成一次可见卡顿。所以命令行
在前，shell API 只在 explorer 起不来时兜底。新增
`tests/test_open_folder_quoting.py`（15 条），把这个顺序也钉住。

### 图片转可编辑文字：支持 Ctrl+V 直接粘贴

截图工具（Win+Shift+S）产出的图只在剪贴板里，此前必须先存一次盘再拖进窗口。现在
在窗口里按 **Ctrl+V** 即可，旁边也加了「粘贴剪贴板」按钮（快捷键再方便，看不见就
等于没有）。剪贴板里如果是复制的图片**文件**，直接用原文件；如果是位图数据，落成
无损 PNG 再走原有流程（识别管线吃的是路径，且再压一次 JPEG 只会伤识别率）。

两个容易忽略的细节：临时图存进缓存目录（那里本就被索引扫描排除，否则截图会混进
搜索结果），且只保留最近 8 张；粘贴图没有"原图所在目录"可用，另存默认位置改成
桌面，不会把成果藏进缓存目录。新增 `tests/test_imgtext_paste.py`（10 条）。

### 解压失败：真因是 MAX_PATH，顺带把产物名的空格也去掉

先说结论：**空格不是主因，路径长度才是。** 一开始按用户描述以为是空格，实测才
定位到真因——把 v1.5.2 的包解到一个 149 字符的目录，解压在最深的那个文件上炸了：

```
FileNotFoundError: [Errno 2] No such file or directory:
  '...\PPT-Doctor\_internal\lxml\isoschematron\resources\xsl\
   iso-schematron-xslt1\iso_schematron_skeleton_for_xslt1.xsl'
```

失败**只发生在最深的几个文件上**，前面两百多个都解得出来。所以用户看到的是「解压
到一半报错」或者「解出来跑不起来」，很难联想到路径长度——也就容易归咎于那个显眼的
空格。

算一下就知道有多紧：Windows 的 MAX_PATH 是 260，包内最深条目 112 字符，留给用户
解压目录的只剩 **147**。而「下载目录 + 压缩包同名文件夹 + 包内 `PPT-Doctor/`」本身
就要吃掉几十个字符，中文用户名再占几个。

最长的 6 条全部来自 `lxml/isoschematron/`——lxml 的 Schematron 校验资源（9 个文件
176 KB）。本项目只经 python-pptx 用 `lxml.etree` 读写 OOXML，全库 grep 无任何引用。
第 7 条是 `qtvirtualkeyboardplugin.dll`：它 import 的 `Qt6VirtualKeyboard.dll` 早就
在 spec 里被剔除了，读 PE 导入表确认过——**这个插件在任何已发布的包里都从未加载
成功过**，纯属占位。两棵树一起删：

| | v1.5.1 | v1.5.2 |
| --- | ---: | ---: |
| 包内最深条目 | 112 字符 | **79 字符** |
| 解压目录可用长度 | 147 字符 | **180 字符** |
| 文件数 | 261 | 252 |

同一个 149 字符的目录，改前解压报错，改后 0.6 秒解完、解压副本的冻结自检 26/26 全过。

**顺带把空格也去掉**：程序目录与可执行文件改成 `PPT-Doctor` / `PPT-Doctor.exe`。
这个不为解压，是为后面每个「自己拼命令行」的环节——批处理、快捷方式目标、安装
工具的调用串，以及上面那个 `explorer /select,`：**根因是同一条引号规则**。

所以程序目录与可执行文件统一改成 `PPT-Doctor` / `PPT-Doctor.exe`。**展示名仍然是
「PPT Doctor」**（写在 FileDescription 里，任务管理器、属性页、界面标题照常）。

改名的代价集中在增量更新：旧名字会进 deletes，helper 如果还按当前进程名重启，就
会去启动一个刚被自己删掉的文件，用户看到的是「更新完程序就打不开了」。所以清单
新增 `entry` 字段记录入口 exe，重启目标以**新版清单**为准。`entry` 来自网络且会被
`Start-Process` 直接执行，因此只认「纯文件名 + 确实在本次落地的文件集合里」，
不符合就退回当前 exe 名（最坏等于旧行为）。老清单没有 `entry` 字段时行为不变。

开机自启不受影响：启动快捷方式本来就在每次启动时按 `sys.executable` 无条件重写，
更新后 helper 拉起新 exe 即自愈。**桌面上手动建的快捷方式需要重建一次**——这是
改名无法避免的一次性代价。

新增 `tests/test_dist_name_has_no_space.py`（17 条），把「常量 / spec / 版本资源 /
打包脚本 / installer」五处对齐，并覆盖改名当天的更新路径与恶意 `entry`；
`tests/test_dist_path_budget.py`（4 条）把最深条目的预算钉在 85 字符。

还修好了官方打包脚本：`tools/package_dist.py` 的防泄漏闸不认识 spec 里有意打进去的
`blank_16x9.pptx`（图片转文字的空白母版），一律拒绝打包，于是这条路一直跑不通、
只能手工压 zip——**v1.5.1 的分发包因此漏掉了 `manifest.json`**（首次启动得自己重算
一遍全量哈希）。现在按精确相对路径放行这一个文件，新混进来的 pptx 照样拦。

## v1.5.1

### 联想加硬预算：最慢查询 51 秒 → 0.29 秒

联想上线后被复审抓到一个发布级性能问题：它把「结果 ≤40 条」当触发条件，而打分是
纯 Python 的编辑距离滑窗、既没有预算也不查取消。真机实测（1,649 份 PPT 的小库）：

| 查询 | 修前 | 修后 |
| --- | ---: | ---: |
| `zzqqxx-nonexistent`（零结果） | **51,464 ms** | **289 ms** |
| `network slice latency`（零结果） | 45,291 ms | 295 ms |
| `汇报.pptx`（3 条） | 817 ms | 291 ms |
| `人工智能`（5 条） | 662 ms | **0.7 ms** |

全文件范围（101,825 条）：6,113 ms → 274 ms、7,904 ms → 261 ms。严格路径无回归
（`汇报` 3.9 ms、`the` 28.6 ms）。**联想能力一条没砍**：`resmue`、`算力方按`
照样召回，别名与型号数字守卫全部保留。

四处改动各对着一个成因：

- **滑窗改成锚点驱动**。原来对每个候选名把所有宽度接近的窗口全试一遍，是
  O(名字长度 × 查询长度²) 的纯 Python DP——profile 显示 `_osa_distance` 被调
  458,054 次、`min()` 一亿六千万次。锚点本就是查询的 n-gram，编辑距离 ≤2 的相似串
  必然保留至少一个，所以只在锚点命中处开窗。
- **统一预算 `RelaxBudget`**：墙钟 250 ms + 工作量 30,000 次 + 取消，三条打分路径
  共用一份。每 256 次才看一次表，免得检查本身变成开销。
- **拆开两个触发门槛**。别名便宜（几次普通检索）、模糊很贵，不能共用 40 这个宽
  门槛——用户边打字边搜的每个中间态都会付一次最贵的路径。模糊与建议库收到 ≤3 条。
- **`suggest_queries` 加上界剪枝**。它要给 1,200 页正文的每个词跑 OpenCC 归一化 +
  `SequenceMatcher`（实测 67,263 次）。用 difflib 自带的 `real_quick_ratio()` /
  `quick_ratio()`（都是 `ratio()` 的严格上界）先挡一道，扣掉最大加分后仍是必要
  条件，召回一条不少。

**取消真正生效了**：50 ms 置位 → 73 ms 返回（修前 1 秒置位仍跑满 7,338 ms）。
联想打分是纯 Python 循环，`conn.interrupt()` 管不到它，所以预算对象把墙钟、
工作量、取消三件事一起管住。

新增 `tests/test_search_relax_budget.py`（13 条）。钉的是**工作量**而不是墙钟——
墙钟随机器快慢飘。整套 1,564 个用例当时全绿却带着 51 秒的查询，就是因为没有任何
一条断言过延迟。

### 「图片转可编辑文字」补上主窗口入口

它原来只在托盘右键菜单里，主窗口和设置页各一个入口都没有——做这个功能的人自己
都没找到它。现在导航轨底部动作区多了「转字」按钮，走的是与托盘菜单完全同一条
路径（`app._open_imgtext_window`），所以「重复点击只把已开的窗带到前台」的行为一致。

### 独立审阅修复：搜索联想、实时索引与 Office/WPS 共存

- 搜索新增严格分层的自动联想：原词严格命中永远在前；结果不足时才补同义别名和
  Unicode 字符近似候选。`百度云` 可联想到 `百度网盘`，普通文件与 PPT 文件名/正文
  共用同一排序契约，联想结果在界面明确标注，不会伪装成精确命中。
- “大小 / 路径 / 时间”排序下推到搜索后端，在截取前做全局 top-K，不再只重排相关度
  先取出的 200 条。
- NameStore 格式升到 v4：size 改为 u64；修复大于 4 GiB 的文件被 `size:` 漏搜、
  `empty:` 混入所有文件夹、Unicode 正则预筛漏结果、DST 日期边界与正则不可取消。
- 实时 overlay 改成按脏目录替换并只保存相对全量索引的真实 delta；重复修改会刷新
  size/mtime，达到上限会显式安排全量校准，不再静默停止收录。
- 区分 Microsoft PowerPoint 与占用兼容 ProgID 的 WPS；WPS 在场但没有 POWERPNT 时，
  允许启动经 PID/父进程审计的独立 Microsoft PowerPoint 预览。renderer 失败写入独立
  日志并通过 IPC 返回原因。
- 任意 HTML/ZIP/代码文件不再被详情区误标为“PDF 内容已全文索引”。

## v1.5.0 发布补记 — 2026-08-30

### 全文件搜索：深度压测抓出的三个静默错误

三个都不报错，只是结果不对——所以只能靠「索引扫描 vs 逐条判定」的全量对账把它们逼出来。

**一、`case:` 和带变音符号的通配符恒返回 0 条。** 预筛必须是**必要条件**，比真正的判定更严就会静默漏结果，而这两处都严过头了：

- `case:README` 拿保留大小写的针，去扫**归一化过**（小写）的名字块——永远找不到。
- `*café*` 的通配符片段用 `normalize()` 筛（不折变音符号），而名字块用 `fold()` 建（折了）——同样永远找不到。

真盘 207 万条对账：43 条查询 × 3000 个样本里 `case:README` 漏了 10 条。修法是让预筛也走 `fold()`——「原样命中」蕴含「折过之后也命中」，所以折过的针仍是合法的必要条件，大小写的真正判定留给逐条 `match()`。

**二、换索引时会撞 WinError 5，而且概率极高。** `os.replace` 要对目标拿 DELETE 权限，但 Python 的 `open()` 不带 `FILE_SHARE_DELETE`——只要有搜索线程正在读那个指针文件，替换就失败。实测：

| 并发读线程 | 换库次数 | 失败 |
| --- | --- | --- |
| 3 | 8 | 1 |
| 6（死循环搜） | 40 | **39** |

失败的后果是整轮全盘扫描白干、索引停在上一份，用户完全看不出来。改成「先退让重试几次，仍抢不到就就地重写」：就地写最坏让某一次查询读到半截名字、当次降级成空结果，而两代文件名长度恒等（`<kind>-` + 13 位毫秒戳 + `.idx`），半截读到的只能是旧名、新名或一个不存在的名字，全被 `current_data_path` 的校验兜住。**修后 120 次换库 0 失败**（同样 6 个读线程、期间完成 5,173 次搜索）。

**三、打全名字反而排不到第一。** 搜 `parse.cjs`，第一条是 `parse.cjs.map`；`index.js`、`report.txt` 同样中招。原因是「完全匹配」这一档只拿去掉扩展名的 stem 判：`parse.cjs.map` 的 stem 恰好是 `parse.cjs`，评到最高档；真正叫 `parse.cjs` 的那个 stem 是 `parse`，评不上、掉到 `partial`。硬分层压过分数，于是正确答案被压在下面。改成 stem 与全名都判，两者任一命中即算完全匹配。**只动「全部文件」这条路，PPT 内容搜索的分级一行没改。**

### 首次建库不再越写越慢：内容索引从 O(n²) 拉回线性

深度压测里量出来的：`db.upsert_file` 和 `db.replace_pages` 每写一份文件都发一句
`DELETE FROM <fts> WHERE file_id=?`，而 `file_id` 是 FTS5 的 **UNINDEXED** 列——每删一次就是一趟全表扫。库越大越慢，写入整体塌成 O(n²)。

盘点那条路（`indexer._write_filename_only_batch`）当年为同一个原因专门改过批量写法，内容类文件走的这条一直没改。库里只有一两千份 PPT 时看不出来；打开文档搜索之后 docx / xlsx / txt / pdf 全算内容类，量级完全不同。

**本机实测（真实代码路径）**：

| | 修前 | 修后 |
| --- | --- | --- |
| 连写 24,000 行文件记录 | 4,805 → **392** 行/秒，30.4 s | 15,451 → **11,837** 行/秒，**1.9 s** |
| 连写 6,000 份 PPT（各 10 页） | 406 → **29** 份/秒，111.7 s | 1,165 → **1,158** 份/秒，**5.5 s** |

末档/首档吞吐比从 0.08、0.07 拉到 0.77、0.99——曲线拉平，也就不再是 O(n²)，规模越大省得越多。

改动只有一处判断：**新插入的那一趟不发 DELETE**。`files.path` 有唯一索引，upsert 前先探一下这行在不在（O(log n)）；页那边用 `pages_raw` 的主键探（同理）。id 来自 `AUTOINCREMENT`、不回收，所以「新 id 必无残留 FTS 行」成立。重扫同一个文件时 DELETE 照发，行为一个字节没变，`tests/test_db_write_scaling.py` 用 SQL 跟踪把这条不变量钉死了。

### 全文件搜索换引擎：照 Everything 的设计重做，索引小 6.9 倍

上一版把「全部文件」范围做成了往 SQLite 里塞 175 万行盘点数据，代价 **1.19 GB**。那是把两个不同的问题硬塞进同一张表：「按名字找任意文件」和「搜 PPT 里写过的字」需要的数据结构根本不一样。这一版给前者做了专用引擎。

**真机实测（2,071,552 个条目：文件 1,818,886 + 文件夹 253,069）**：

| | SQLite 旧做法 | 新引擎 |
| --- | --- | --- |
| 索引体积 | 1,219 MB | **177.8 MB**（6.9 : 1） |
| 常驻私有内存 | 随库走 | **+10.7 MB**（mmap，其余由系统按需分页） |
| 名字查询延迟 | — | **25–45 ms** |
| 取前 200 条完整信息 | — | 32 ms |

> 体积从最初报的 131.6 MB 涨到 177.8 MB，是后来为 `path:` 查询加了一段「目录路径的归一化形式」——那一段把路径查询从 27.7 秒压到 0.23 秒，这 46 MB 花得值。

省下的 1.04 GB 来自两处，都是 Everything 的做法：

- **目录只记一次**。182 万个文件只分布在 25 万个目录里，平均 7 个文件共用一个目录。SQLite 每行存一遍完整路径（平均 112 字符），同目录下那一长串前缀被完整重复了 7 遍。改成「目录表存一份，文件只记 dir_id + 自己的名字」。
- **不建倒排索引**。SQLite 那边要 `file_names_fts` + `name_norm` + `idx_files_name` 三套结构（合计 195 字节/行）才能让「打三个字立刻出结果」不退化成读盘全表扫。新引擎把归一化名字打包成一块连续内存，用 `bytes.find` 从头硬扫——38 MB 扫一遍 13~25 ms，比建索引再查还省事，那 195 字节一分不花。

Everything 还有第三招**学不了**：直接读 NTFS 的 MFT。那要管理员权限，而本程序是绿色免安装、不提权跑的。但那一招决定的是「扫得多快」（它 2 秒，我们 104 秒），不是「占多大」——而那趟扫盘为了找 PPT 本来每周就在做，所以并不多付代价。

**顺带补齐了文件夹**。Everything 能按名字搜到文件夹，之前只索引文件。现在文件夹和文件在同一张记录表里（靠一个标志位区分），双击文件夹就在资源管理器里打开它。

**PPT 相关的一切行为一个字节没变**：内容检索仍然走 SQLite，那条代码路径一行没动，并有专门的守卫用例钉住（内容结果永远 `is_dir=False`、`file_id` 永远是真实正数）。

#### 实现里踩到并修掉的四个坑

- **Windows 不让替换正在被 mmap 的文件**。就地覆盖索引会得到 `WinError 5`——后果是：用户只要搜过一次「全部文件」，此后每轮建库都**静默地**装不上新索引，索引永远停在那一刻。改成「版本化数据文件 + 一个很小的指针文件」：每次重建写一个新名字，再原子替换指针（指针从不被 mmap，替换总能成功），老文件等没人用了再顺手清掉。
- **孤立代理项的文件名会让 OpenCC 直接抛异常**。真机上足以把整轮扫盘炸在半路。归一化前先剥掉（与 SQLite 那条路同一个 `sqlite_safe_text`），但**原始名字原样保留**，否则交回去的路径打不开那个文件。
- **`file_id` 必须是互不相同的负数**。下游有两处直接 `WHERE file_id=?`（页标题、复制本页文字），还有两处拿 `f"s{file_id}"` 当归组桶键。全体共用一个 id 会让所有结果塌进同一个桶、被整体提到首条位置，排序当场作废；正数则可能撞上某个真实 PPT 的行，把别人的内容显示成这个文件的。
- **去扩展名不能只认 `.pptx`/`.ppt`**。内容搜索的 `_stem_name` 只剥 PPT 扩展名，照搬过来的话在全文件范围里搜 `report` 永远排不到 `report.txt` 第一，会被一个更新的 `report-2026.txt` 压下去。新路径按「最后一个点」剥，不动原有内容搜索。

**已删除的文件不会再赖在结果里**：平铺索引是整份重建的，两次重建之间删掉的文件仍在里面。与其为删除单独维护一套增量账本，不如在**要显示的那几条**上顺手 `stat` 一下（通常正好 200 次，几毫秒），边过滤边取够条数。

**迁移**：老版本开过「索引所有文件」的用户，升级后启动时会在后台把 SQLite 里的历史盘点行清掉——那正是 1.19 GB 的来源。内容类型的行不在清理范围，PPT 一行不动。

### 全文件搜索：按「不比 Everything 差」逐项对齐

#### 实时更新

Everything 的实时靠 **NTFS 变更日志**，那要管理员权限；它自己也内置了第二套——**目录变更通知**，用于拿不到日志权限或非 NTFS 的场合。本程序绿色免安装、不提权，所以照搬它这套备用机制：watcher 报出变化的目录 → 重列该目录 → 并入一份**同格式的增量层索引**，搜索时两份一起扫、按路径去重。

不去改那份全量索引是有原因的：它是只读 mmap、整份重建的。删除也不必单独记账——结果在显示前会逐条 `stat`，已经不存在的自然消失。

#### 查询语法（新增 `namequery.py`）

| | |
| --- | --- |
| 通配符 | `*.pdf` `a?c` —— 用了通配符就是**匹配整个名字**（与 Everything 一致：`abc` 是包含，`abc*` 是以 abc 开头） |
| 布尔 | `a\|b` 或 · `!b` 非 · `<>` `()` 分组 |
| 扩展名 | `ext:pdf;docx` |
| 大小 | `size:>10mb` `size:1kb..10kb` `size:empty\|tiny\|…\|gigantic` |
| 时间 | `dm:today` `dm:lastweek` `dm:2026-08` `dm:>2026-01-01` `dm:2026-01..2026-06` |
| 路径 | `path:foo`；**查询里出现 `\` 或 `/` 时该词自动改成匹配完整路径** |
| 其他 | `folder:` `file:` `regex:` `case:` `ww:` `empty:` |

**PPT 内容搜索那套语法一个字没动**。两者的用户习惯完全不同——在内容搜索里 `*` 是分隔符，在文件搜索里是通配符，混用只会互相拖累。有专门的用例钉住这一点。

没做的（`dupe:` `attrib:` `parents:` 等）逐条写在 `namequery.KNOWN_GAPS` 里，不假装支持。

#### 压测：最慢 27.7 秒 → 0.98 秒

先按语义写对，再逐条压。真机 **200 万条**：

| 查询 | 最初 | 现在 |
| --- | --- | --- |
| `src/pptx_finder` | 27,747 ms | **230 ms** |
| `path:pptx_finder` | 27,746 ms | **235 ms** |
| `<a\|b> ext:png` | 7,055 ms | **545 ms** |
| `size:>100mb` | 6,441 ms | **618 ms** |
| `dm:today` | 6,397 ms | **594 ms** |
| `regex:^\d{4}` | 5,847 ms | **692 ms** |
| `folder:` | 2,234 ms | **254 ms** |

四处提速，全部是「别做无谓的解码」：

- **只看大小/时间/是否目录的查询**（`size:` `dm:` `folder:`）直接遍历 20 字节定长记录，一个字符串都不解码。
- **目录的归一化形式挪到建库时算**。查询时算的话，25 万个目录每次都要现跑一遍 NFKC + OpenCC。存进索引只多占 13% 体积，而且路径匹配可以直接在那一段上做 C 速度的子串扫描。
- **路径查询先用目录表预筛**：路径 = 目录 + 名字，而目录只有 25 万个、被 200 万个文件共享。
- **正则在名字块上整块跑一遍**再回查序号，而不是逐条取名字比。

**正确性交叉校验**：3000 条随机样本 × 25 条查询，索引扫描的结果集与「逐条用解析器判定」的结果 **0 不一致**。

#### 期间修掉的一个静默 bug

加了新段之后头部结构超过预留的 128 字节，而 `header[:len(packed)] = packed` 会把 bytearray **撑长**——头部悄悄盖掉第一个段的开头，所有偏移错位，**不报错**，只是搜出来的东西是乱的。头部扩到 256 字节并加了断言，以后再加段会当场断在测试里。

#### 结果显示 · 排序 · 其他对齐

- **结果卡补上大小和所在位置**。原来只有文件名和修改时间——「找到了但不知道在哪、多大」，而同名文件散落在十几个目录是常态。
- **排序新增「大小」「路径」两键和反向按钮**。按大小揪出占地方的大文件是这类工具最常见的用法之一。
- **忽略变音符号**：打 `resume` 能找到 `résumé`（Everything 的 Ignore Diacritics）。只加在文件搜索这一层，PPT 内容搜索的归一化口径没变。
- **可移动磁盘纳入自动索引**（U 盘、移动硬盘）。拔掉之后那些条目会在存在性检查里自然消失。网络盘仍需在设置里显式添加——一次全盘遍历可能要走很久的网络。
- **语法在界面上说出来**：切到「全部文件」范围时，输入框提示语和悬停说明换成 Everything 语法速查，查询提示条轮流展示 `*.pdf` / `ext:pdf` / `size:>10mb` 等写法。藏着不说等于没做。

## v1.5.0 — 2026-08-28

### 全文件搜索：能力常开，范围在搜索框旁边选

**问题**：设置里有「索引所有文件（Everything 式文件名搜索）」这个开关，但打开之后仍然搜不到任意文件。原因是两层：开关默认关，而且开启后要等一轮全盘重扫才有数据；即使数据到位，入口也藏在**另一个**下拉（搜索模式）的最后一项里。一个要先找到开关、再等一轮重扫、再去另一个下拉里换模式才能用上的能力，等于没有。

**改法**：

- **开关撤掉，能力常开**。全盘文件名盘点不再是可选项，装好就有。刻意不再读 `ui.json` 里的旧键——留着认那个值只会留下一个死结：从前手滑关过一次的用户升级后永远开不回来，而界面上已经没有地方能让他开回来。
- **搜索框右边的下拉改成「搜索范围」**：`PPT` / `Word` / `PDF` / `全部文档`（后三个仍跟随「Word / PDF 内容搜索」开关）/ **`全部文件（仅文件名）`**。最后一项永远在，永远在最后——不抢 PPT 的默认位。
- **限制写在标签里**。全盘盘点只登记文件名、不解析内容，所以这个范围下只能按名字找。与其等用户搜不到再去猜原因，不如把「仅文件名」四个字直接写进选项。选中它时，旁边的「模式」下拉自动置灰（那里的「仅内容」在这个范围下没有意义）。

**代价重新实测过，比旧注释里写的贵**（旧注释说约 500 MB）：

| 项目 | 实测 |
| --- | --- |
| 剪枝后要登记的文件数 | **1,750,775** 个（其中 PPT / Word / PDF 只有 **2,155** 个，813 : 1） |
| 平均路径长度 | 112 字符 |
| 单行成本 | **731 字节**（路径 134 B、文件名 FTS 110 B、name_norm 51 B、两条索引 60 B，其余是 files 行本身） |
| 索引库增量 | **约 1.19 GB** |
| 首轮额外入库时间 | 约 1.8 分钟（磁盘遍历本来每周就在做，这是**额外**的写库时间） |
| 升级后首次启动 | 特征签名从 `any_file=0` 变成 `1`，会安排一轮全盘重扫（约 2.4 分钟遍历 + 1.8 分钟写库，低优先级后台跑，期间照常能搜） |

拆解过一遍，**没有便宜的减法**：占比最大的三块（路径原文 23%、文件名全文索引 19%、归一化文件名 9%）都是「按名字找文件」这件事本身要付的钱，砍哪块都会直接降低搜索质量。真要再省，只有缩小收录范围（多排除几类构建产物目录）这一条路——那是产品口径的取舍，留给你定。

这笔钱值不值：省下的 1.19 GB 用户感觉不到，「装了 Everything 能力却搜不到任意文件」的挫败感天天都在。想要轻量的人把范围留在 PPT，索引和搜索都不会因此变慢（内容检索只查 `pages_fts`，盘点行不参与）。关闭路径（清理盘点行、增量对账）的代码原样保留，只是不再有界面入口。

### 界面字号：三档之外可以自定义

原来只有小 / 标准 / 大三档（90% / 100% / 115%），跨度窄、也不一定合每个人的屏幕。现在保留这三档一键即得，另外加一项「自定义…」，配一个 **0.50~2.00** 的倍率框（步进 0.05）。选预设时倍率框置灰并同步显示该档数值，不给两个互相打架的输入；存的值不是预设档位时，打开设置直接落在「自定义」项上回显原值。

**搜索行跟着字号一起长**。这不是顺手做的：搜索框和两个下拉原本写死 34 / 30 / 30 像素，倍率一放开就会把字切掉半截——只改 config 不改 UI，等于给了一个开到一半就坏掉的滑杆。现在按控件实测行高 + 内边距算高度，并保留原来的下限，所以 **100% 及以下逐像素还是老样子**（34 / 30 / 30），只有放大时才会长高（200% 时 51 / 44 / 44）。

这里有个容易写错的地方记在代码里：倍率是通过重写 QSS 的 `font-size` 生效的，除非用户另设了字族，否则应用级 `QFont` 根本不变——照 `app.font()` 去量行高会得到一个恒定值，等于这段代码什么都没做（第一版就是这么写的，实测六个倍率下高度纹丝不动才发现）。必须量控件自己的 `fontMetrics()`。

两端刻意夹死：0.5 以下 13px 的基准字号会掉到 6px，2.0 以上侧栏和卡片开始互相挤——再往外已经不是「能用」的界面了，与其让用户踩坑不如在入口处挡住。超范围的数值现在**夹到边界**而不是像以前那样一律砸回 100%（用户要的是「更大」，就给他最大的合法值）；但根本不是数字的值（字符串、布尔、NaN、inf）仍然回落 100%——那类值会一路流进 QSS 生成 `font-size: nanpx`，让 Qt 拒收整张样式表，而配置已经落盘，等于每次启动都裸奔。

### 版本库：文本零件压缩存储（可省 788 MB）

版本库把每份 PPTX 拆成零件（part）内容寻址存放，此前一律不压缩。对真实版本库做了一遍只读盘点：

| | |
| --- | --- |
| 对象池 | 48,775 个对象 / 3,175 MB（4,101 份 manifest） |
| 文本零件 `.xml` / `.rels` | **889.9 MB（28.0%）**，但**个数占 77%**（37,466 个） |
| 媒体零件 | 2,090.5 MB（65.8%） |
| 抽样 600 个文本零件实压 | 11.6 MB → 1.3 MB（**11%**） |
| 全部压缩后的潜在收益 | 890 MB → 102 MB，**省 788 MB**（版本库总量的 24.8%） |

**这 788 MB 是逐步兑现的，不是升级当天就到账**：存量零件一个不动，只有此后新留底的零件走压缩。你继续改稿、旧版本按保留策略淘汰，版本库才慢慢换血。想立刻拿到这 788 MB 需要一次「存量重打包」，那是另一件事，没有做。

**留底反而更快了**：12 份真稿首次留底 13.7 s → **9.2 s**。原因是文本零件占了对象数量的 77%，它们改走内存路径后每个零件只落一次盘（而且写的是压完的小文件）；媒体零件仍然走原来的流式路径，峰值内存不变。

这是全应用最不能出错的地方（零件的**文件名就是内容的哈希**，校验时会重新哈希比对），所以整个设计是**只加不改**：

- **哈希口径一个字没变**。文件名永远是**未压缩内容**的 xxh64，压缩件只是多一个 `.z` 后缀的存放形态。manifest、跨文档去重、GC 的引用集、深度体检——全部照旧按哈希对账，不认文件名的形态。
- **存量零件一个不动**。读取按 `<hash>` → `<hash>.z` → 旧的每文档池顺序解析，升级前存下的原始件永远命中第一条，既不重写也不迁移。已经在池里的对象即使内容相同，也直接复用原始件，不会多存一份压缩件。
- **压完必须解压回来逐字节比对才允许落盘**。对不上就放弃压缩、退回存原始件。也就是说压缩路径就算彻底坏掉，最坏结果也只是「没压」，不可能往对象池里放进一个解不回原内容的文件。另有一道兜底：每次留底本来就会重组一遍做保真自检，自检不过会退成整份拷贝——测试专门钉住「留底不许因为压缩而退成 mode=full」，因为那种退化不报错、只会让版本库不降反涨。
- **只压 `.xml` / `.rels`，且只压 8 MB 以下**。媒体零件本身已是 PNG/JPEG，压了只是白烧 CPU；上限则是为了不给一个 300 MB 的媒体零件在内存里攒压缩缓冲——超过就当场放弃。压完省不到 25% 也不要，多一层解压不划算。
- **压缩件坏掉算「内容不符」，不算「读不到文件」**。gzip 解不开说明这个零件的字节坏了，体检报给用户的结论必须是「内容对不上」而不是「读取失败」——后者会被误当成一次偶发的 I/O 抖动。

产出用 gzip 流且 mtime 恒为 0：同样的输入永远得到同样的字节。对一个内容寻址的存储来说，可复现比省那十几字节的头部重要。

### 轻量化：磁盘占用

对运行期开销做了一轮实测体检（内存、CPU、存储三条轴），结论是**存储是唯一的真问题**：

- **索引库回收 146 MB**。真机上索引库 132 MB 里 114 MB（86%）是空闲页——数据删了但磁盘没还；事务日志（WAL）攒到 32 MB 也从没回收过。代码里本来就有整理逻辑，但触发条件写成了「空闲空间**同时** ≥256 MB **且** ≥25%」——一个 132 MB 的库无论怎么膨胀都够不到 256 MB，于是这条整理**一次都没跑过**。改成「或」的关系，并给「按占比」这条路加一条 32 MB 的下限（免得为几百 KB 去 VACUUM 一个新库）。WAL 独立判断：攒够 8 MB 就 TRUNCATE 一次（原来只在 VACUUM 之后才截断，而 VACUUM 从不发生）。实测：164 MB → 17 MB，耗时 0.38 秒。
- **预览缓存上限 2 GB → 512 MB**。真机 1548 个缓存文件才 103 MB，其中 174 个 2560px 大图占 72 MB；2 GB 这条线实际等于「不限」。没命中就重渲一次约 1 秒，且只落在很久没看过的文件上。

同轮**查过但没做**的（写下来免得再走一遍）：

- **界面内存**：核心引擎（索引+搜索）只有 60 MB，主窗口跑到 215 MB。三个假设全被 A/B 实测否掉——控件数（1 页 183 控件 193 MB vs 4 页 726 控件 205 MB，4 倍控件只差 12 MB）、样式表（整张去掉仍 215.3 MB）、字体链（全部去掉仍 215.5 MB）。合成测试里字体链曾显示能省 82 MB，但在真实窗口上三次都复现不出来，**已写好的改动因此回退**——不上没有实测收益的改动。
- **CPU**：空闲 0.02%（20 核、30 秒采样），索引 17 ms/文件，搜索 5 次 0.01 秒。整库建索引慢是**设计如此**（低优先级、最多 2 worker、用户一交互就暂停），不是缺陷。
- **版本库文本压缩**：当时算出全库可省约 795 MB，但因为要动版本历史的完整性校验模型而留待单独评审。**已在本次实现**（见上文「版本库：文本零件压缩存储」，复测后的准确数字是 788 MB）。
- **版本库全文索引去副本**（省 32 MB）：那份副本是「在历史版本里搜关键词」要用的，去掉就没法显示命中片段，不值。
- **预览图 PNG → JPEG**（省 5~10 倍）：幻灯片满是文字和细线，JPEG 会有振铃杂边，预览清晰度比缓存体积重要。

## v1.4.0 — 2026-08-27

### 新功能：图片转可编辑文字

把一张 PPT 效果图变成一页 PPTX，图里的文字成为真正的文本框；图形、图表、配色、版式原样留在背景里。全程离线、无 LLM、结果确定可复现。托盘菜单「图片转可编辑文字…」进入，拖图即可。

**主程序零新增依赖**。图像处理用已有的 `QImage`、写 PPTX 用已有的 `lxml` + `zipfile`（往一份 28 KB 的空白母版里注入背景图与文本框，母版来自真实 PowerPoint 产物，兼容性有保证）。识别模型那套依赖（onnxruntime + opencv + 模型）打包后解压 190 MB，做成**按需下载的独立侧车进程**：基础包仍是约 37 MB，首次使用时下载 80.7 MB（zip 压到 43%），装好后永久离线。侧车崩了也只崩侧车。

选型有实测支撑：24 张真实幻灯片（12 份第三方公开数据集稿，含德/爱沙尼亚/印尼/英语）+ 4 张构造的硬样本，以索引库里的 PPTX 原文为对照。Windows 自带 OCR F1 87.9%，本地模型 F1 95.0%，逐张 19:1:4。差距高度集中在中文稿——欧语两者都是 92–99%，中文稿 Windows 掉到 50–89%。（原始 24 张里有 5 张的基准本身不可用：2 张的 PPTX 文本含画布外/被压盖/溢出裁切的内容，3 张的幻灯片里嵌了带字的图片而 OCR 反而读得出来，这 5 张按同一标准剔除并如实计数。）

管线里每一步都由踩过的坑推出来，注释里记了现场：

- **墨迹测量**：OCR 检测框的留白不固定，同版同号字能给出 27~39 像素的框高，直接用会让字号忽大忽小。改为在框内扫真实墨迹外接矩形。
- **背景补洞按列插值**：早期整块填众数色，渐变底上留下肉眼可见的矩形补丁。改为按列在上下两侧各找一段干净背景再垂直插值——纯色、横向渐变、纵向渐变都能连续还原。采样必须同时满足「平滑」和「远离字色」：只判平滑时，一根竖直笔画沿列方向同样恒定，会被当成背景把墨色一路涂下去（表现为一列列彩色竖条纹）；只按固定偏移取样时，行距小于 6 像素会直接压在相邻一行的文字上。
- **色块守卫**：检测框略微越过色块边界时，环外采样拿到色块外面的底色，于是色块本身成了「墨」——红圆圈里的白数字就是这样被整块擦掉的。用墨迹覆盖率识破：710 处真实文本中位 0.32、95% 分位 0.55，而所有判反样本都 >0.67。命中即整处跳过，像素原样保留。
- **字号反解**：用真实字体度量二分。宽度校正只对三字及以上生效——单字符的宽度完全由字形决定，硬套会把数字「3」砍掉 40% 字号。
- **最小字号**：低于 6pt 的留在背景图里不做成文本框（图表坐标轴刻度、模板占位符）。阈值别往上调：密集排版稿的正文就落在 7~9pt，设 9.0 会一次砍掉 41/58 处真实正文。
- **字重**：同版笔画宽度相对比较，只判 12pt 以上。381 条真实字重对照上准确率 100%、召回率 30%——**零误判**是这里唯一不让步的指标，「该细的被加粗」看起来是坏掉，「该粗的没加粗」只是不够醒目。此前两次尝试都失败并记在代码里：墨迹占空比同时被字形与字号污染；渲染参照法在 offscreen 平台上 `QFont.setBold` 根本不生效。

刻意**不做**的两件事：不做校对台（产出本身就是可编辑 PPTX，改错字在 PowerPoint 里最自然）；不做「自动标出可疑处」（实测行不通——OCR 的错误高度稳定，四趟不同超采样错得一模一样，多趟比对召回率只有 22%）。

组件下载复用了自动更新那套安全约束：先验整包 sha256、解开后再逐文件校验、包内出现清单之外的文件即拒绝、每条路径都过越界守卫，全部就绪才原子换入——中途失败或取消绝不留下半套组件。

#### 第二轮：换 6 张没测过的新图，又挖出五个问题

前一轮的 28 张自测集没覆盖到几类很常见的版式。换 6 份**从没用过**的第三方稿重测，问题一个个现形，全部修掉：

- **多行文字终于是一段，不是一堆碎片**。OCR 按视觉行给结果，此前每行各自定字号、各自按墨迹左边缘定位，于是「彩色块里的多行居中文字」散成了忽大忽小、参差不齐的几行——而这是 PPT 里最常见的版式之一。现在垂直相邻、同列、同色、字号相近的行并成一段：共用一个字号、由各行边缘的离散度反推对齐方式（左/中/右）、整段排进一个文本框、行间用显式换行（原稿的断行位置一个字都不变）。归段判据全部实测定档：143 对相邻行的「行间空白 / 行高」在 1.0~1.3 之间有明显的谷（60% 分位 0.68、75% 分位 1.32），阈值取在谷底。28 张回归：686 行归成 447 个文本框，零过度合并。
- **表头的白字不会再把正文的黑字染成白底白字**。归段一开始只看几何，于是表格的蓝底白字表头和紧挨着的白底黑字首行被判为同一段——几何上它们确实同列、间距小、字号近。整段按第一行的白色排字，白底上的黑字**肉眼直接消失**。加了字色判据后分开了。
- **照片不会再被刷成一片白方块**。书本特写上 OCR 认出的十几个零散词被当成正文抽走，补洞找不到干净背景就整块填单色，把照片糊掉一大片——这是明确的画面破坏，比不抹更糟。判据不能用「能不能找到平滑的一段」：**虚化的照片本身就是局部平滑的**，那个判据在实拍图上一路给满分，一个都拦不住。改用「相邻列背景采样的色差」——纯色底、横向渐变、纵向渐变在横向都是连续的，照片则每列都不同。685 处实测：95% 的中位列差是 **0.0**，99% 在 1.7 以下；超过 4 的只有 2 处，都是 logo 里压在红橙纹理上的微型白字（跳过它们本来就对）。
- **色块下沿不会再刷出一道横向色带**。表头色块常在文字下方几像素处就结束，向下采样于是拿到色块**外面**的颜色，插值就在本该纯色的地方刷出渐变。本处的环外底色是可靠的先验，谁离它远谁就是越界的——只在「一个明显远、另一个明显近」时才丢，两个都远说明底本来就花（照片、强渐变），插值仍是最优解。
- **多行段落不再整体下移二十多像素**。此前行距给的是「相对单倍行距的百分比」，而单倍行距是字号的多少倍由字体的 ascent/descent/lineGap 决定、各字体不同，按 1.2 猜的结果是项目符号（留在背景图里的图形）和文字明显对不上。改成给**绝对行距**，并用一份 PowerPoint 落点标定反解框顶（基线 = 框顶 + 行距 − descent）。定量复核 52 段：中位偏差 0px，90% 在 2px 以内。横向也顺手修了——此前一律 `left = x0 - pad`，每个左对齐的段都整体左偏一个 padding。

再换一张全新的稿做最终验证，又抓到两个：

- **单行的字重误判不会再放大成整段**。归段后段的字重取的是 `any(行判粗)`，于是一页 12 行的常规正文里只要有一行短句被误标，整段 12 行全排成粗体——而「零误判」是字重这块唯一不让步的指标。改成段内多数票：真正的粗体段落每一行的笔画都粗，多数票同样成立；单行段落的行为与合段之前完全一致。
- **竖排的序号不会再被拍平成一串**。OCR 会把竖着排的几项并成一「行」：序号 1)2)3)4) 被返回成单个 36×111 的竖框、文字 `"234"`，照排就成了横着的 234。凡是「墨迹比自己还高、且不止一个字」的一律整处保留——883 处两字以上的墨迹里宽高比低于 1.0 的只有 2 处（0.32 与 0.87），下一档就跳到 1.30，阈值取在这个空档上。

还有三处是在逐张目视复核时发现的：

- **抹字后不再留下细竖条纹**。向外找干净背景时会撞上**相邻一行的抗锯齿边缘**——那种半灰像素沿列方向同样平滑，离字色也足够远（实测 226 对字色 89、底色 243，距离比 0.79），怎么调「远离字色」的门槛都拦不住。但这类污染是**孤立的单列**，把逐列采样沿 x 做一遍中值滤波正好对症；真渐变沿 x 平滑，中值滤波近乎恒等，不会被抹平。修完之后一行 559 列全是同一个底色，一个离群点都没有。
- **识别门槛从 0.50 提到 0.60**。原来的理由是「宁可多出一个可以一键删掉的文本框」，但这句话漏了一件事：**误检不是免费的**——它会把那块原始像素一起擦掉，删了框就留下一个洞。实测大学校徽上被凭空认出 `'同同'`(0.51) 和 `'可可'`(0.57)，照排就把校徽盖掉了。966 处实测，0.60 以下只有 5 处（0.5%），其中 4 处是明显的幻觉；而真实正文最低也在 0.72，门槛离那条线还有距离。
- 转换窗口的结果提示改为按**文本框数**报（多行合成一个框之后，框数不等于行数，两个都报），并说明新增的跳过原因。

#### 第三轮：公式整处保留原样

用户实测反馈：公式转得不好。根因是**结构性**的——上标、下标、帽子、根号是二维排版，而这条链路只会把一行字排在一条基线上；OCR 又会把 `σ` 读成 `o`、`I` 读成 `1`、上下标拍平，于是 `ĥ = R_MSG S^H ( S R_MSG S^H + σ²I )^{-1} y` 排出来是 `h=RMSGSH` + `SRscS+o²1）-1` 两串没人看得懂的字符，还把原来漂亮的公式擦掉了。

**先说四个失败的判据**（都实测过，都不成立，写下来免得有人再走一遍）：

- **基线几何**（量「墨迹是否坐在同一条基线上」）：公式落在 0.18/0.33，正常文字最高到 0.67，完全重叠。字形切分会把 `R_MSG` 的下标和 `R` 连成一块，切不开。
- **OCR 置信度**：最反直觉的一个。拿 24 张基准稿的 PPTX 原文逐条比对，0.80~0.99 区间的读对率是 83% / 69% / 55% / 57% / 58%——**几乎不相关**。门槛提到 0.85 会误伤 12 条读对的、只除掉 7 条读错的。所以 0.60 这个门槛不动。
- **墨迹密度**：公式那两行是 0.14/0.15，但西文小写正文天生稀疏（0.128、0.133、0.144 全是正常句子），绝对值分不开。
- **行分布匹配度**（源图墨迹的竖直分布 vs 我们排出来的分布）：`h=RMSGSH` 反而比平均更「像」——OCR 已经把它拍平成 8 个普通字符了，信息就没了。

**最后成立的是字符判据**：希腊字母、`∑∏∫√∂∞≈≤≥±`、上下标数字、组合重音（帽子 x̂、横杠 x̄）——这些在普通 PPT 正文里几乎不出现。925 处实测命中 5 处（0.5%），4 处是真公式，1 处是把 "MEC" 读成 "ΛEC" 的错行（跳过它也对）。刻意**不**收 `×`（"3.2×" 是倍数）和 `·`（中文项目符号），这两个一收就是几十个误伤。

再加一条**沿同一视觉行传播**：公式常被 OCR 切成好几段，往往只有一段带得动数学字符。正是这条把 `h=RMSGSH` 和它右边的 `SRscS+o²1）-1` 认成同一条公式。

命中即整处保留——不排字也不补洞，原始像素一个都不动。效果：那条公式的斜体、上下标、帽子、σ² 全部完好，因为我们根本没碰它。

#### 第四轮：毛刺、误合与提速

用户实测又反馈：标题下方偶尔有一小撮红色毛刺。根因是 **OCR 的检测框把标点的尾巴切在了框外**——中文全角逗号「，」的尾巴常拖出框；框外那一截我们从来不看，也就永远抹不掉（实测标题框底边 y=96，逗号尾巴一直到 y=105）。改为**把抹除框沿四边扩到边界干净为止**，两道闸防止跑飞：扩到上限还没干净就整个方向不扩（说明身处密集图形，扩下去会啃掉图形），边界那一行的墨量超过框宽 15% 也停（撞上相邻一行了）。945 处实测：66% 一点不用扩，99% 分位 9px，最大 23px。只扩**抹除**范围，不动测量框——把逗号尾巴算进去会让整行字号偏大。

顺手在用户自己转过的另外三张图上又抓到一个**误合**：图表的顶部居中标题被并进了右侧图例。那一对在四个判据上**全部刚好擦边通过**（间距 1.14 对上限 1.15、水平重叠 0.37 对门槛 0.35、字号比 1.52 对上限 1.60、同色），合完整段被判成右对齐，标题被拉到右边还缩了字号，三条图例线也跟着配错标签。补的判据是「同段各行必须至少共享一条对齐边（左/中/右）」——293 组实际配对实测：中位 0.03、99% 分位 1.77、最差的正常配对 2.00（首行缩进的项目符号续行），而那一对是 3.77，阈值取在空档里的 2.5。

**提速 33%（704 → 468 ms/图）**，两处都是可证明的等价变换，1027 处逐项对拍零差异：

- `measure_run` 里每个像素的「离底色距离」原来要算两遍（一遍定字色、一遍扫墨迹），改为算一次复用。一行大标题的检测框有八万多像素，这是这里最大的开销。
- `paint_out` 的插值色带原来逐像素重算；纯色底上所有列的采样色本来就一样（中值滤波之后更是如此），改为按「上下采样色」缓存色带。

还有两件**查完决定不做**的：

- **PNG 压缩档位**：默认档 307ms / 876 KB，quality=70 是 68ms / 1054 KB。省 0.24 秒换文件大 20% 不划算——PPTX 是要发出去的文档，体积比这点时间重要。
- **中英文之间自动补空格**：拿 24 张基准稿的 PPTX 原文数了 126 处中↔英数边界，**67% 本来就没有空格**。盲目补会错 2/3，所以「不补空格」这条保持不变，现在有数字支撑。

最终回归：40 张（28 张自测集 + 6 张新稿 + 1 张最终验证稿 + 1 张公式稿 + 用户实际用的 4 张）全部转换成功、PPTX 结构零异常，1024 行文字归成 732 个文本框，468 ms/图（不含识别）。

#### 发布配套

- **新增无界面入口** `PPT Doctor.exe --imgtext <图片> [输出.pptx]`。既方便批量转换（写个循环就能一次跑一叠图），也是**打包之后验证这条链路的唯一手段**——GUI 里点不出退出码，命令行才能把「模板找不到」「识别组件没装」「PPTX 结构坏了」变成非零退出码。加 `--install-component` 可在无界面下装识别组件。
- **识别组件下载加了备用源**。组件 80 MB，托管在自建更新源上；那台机器不可达（或还没传）时下载按钮就是死的。现在主源取不到清单就自动回落到 GitHub Release 上的同名资产——同一份清单、同一套两层 sha256 校验，安全性一致。清单里记住是从哪个源来的，压缩包从**同一个源**取，不出现「清单来自 A、包来自 B」的混搭。

#### 第五轮：100 张真实稿压测

从本机 60 个目录里挑了 **100 张真实 PPT 效果图**（以密集中文技术稿为主，一页几十行、行距压到极限、还叠着标注框），做了一轮不靠肉眼的压测。

**怎么量的**（这些图是 AI 生成的，没有对应 PPTX，没有「标准答案」）：

- **文字正确率 = 把结果再读一遍**。把转换出的 PPTX 渲染回图片，再跑一次同样的识别，与原图的识别结果逐字比对。召回 = 原图的字还找不找得到；准确 = 结果里的字是不是原图有的。
- **画面破坏度 = 只看非文字区域**。这个功能只该动文字、不该碰图形，所以把画面切成「文字区 / 非文字区」分开比——非文字区的差异一旦升高，就说明抹字时把图表、配色或版式弄坏了。

**结果**：100 张全部转换成功、零失败；文字召回中位 **98.2%**、准确中位 **99.0%**、F1 中位 **98.5%**；召回 ≥95% 的 83/90 张（10 张是 inpaint 空白底图，不参与统计）；画面改动中位 **1.30/255**、最差 3.63/255（1.4% 灰阶）；中位 811 ms/图。

**这一轮抓到两个一眼能看出来的缺陷，都已修好**：

- **行距紧时文字互相重叠**（影响 24/100 张）。字号是按「量到的文字有多高」反解的，而行挨得近时 OCR 的检测框会连带框住上下行的笔画，量出的字高比真实值大两三成，字号跟着放大，一放大就顶到隔壁行，密集要点列表糊成一团。修法：两行的墨迹框重叠时，在重叠区的**中线**上切开、各让一半，再重算字号——重叠意味着那段像素本来就分不清属于谁，对半分是唯一公平的取法。只处理「啃掉一点点」的情况（实测 7~12px），重叠过半的不动，那是别的情况，乱切会切掉真笔画。
- **同一处叠两遍字**（全语料 11 处）。OCR 偶尔对同一块像素给两个框：一个框整行 `推理token`，另一个只框里面的 `推理`；也有两个不同读法压在一起的。两个都排就是原地重影。修法：相交面积超过较小框六成即判定指向同一块像素（同一块像素不可能同时是两段不同的文字），留置信度高的那个。阈值有数据支撑——5801 处文字里相交的只有 36 对，超过 0.6 的 12 对，其余都在 0.07 以下。

**为什么之前没发现**：原来的自测集 40 张以国际会议稿和欧语版式为主，行距宽松、留白多；而真实使用场景是密集中文技术稿。换成贴着真实用法的测试集，两个问题立刻现形。

**一条给后来人的环境警告**（已写进 `fit_point_size` 的文档字符串）：调字号相关的常量必须在真实平台上量，不能用 `QT_QPA_PLATFORM=offscreen`——离屏平台不加载系统字体，一切度量来自回退字体。同一张图离屏算出的正文是 8.5~9.7pt，真实平台上是 11.6pt，差两成半，据此定出来的阈值全是错的。顺带用真实字体重测了字体选型：微软雅黑按墨高对齐后的宽度比原稿中位 0.93，Segoe UI / Calibri / Arial 都不更贴，所以**不按语种换字体**——那个复杂度买不到保真度。

已知限制（都在识别层，不是排版层能修的，已写进模块文档）：长而低对比的标题偶尔被 OCR 截断，只擦中段会留下半截原文（34 张实测 1 例）；个别行的词间空格与变音符号丢失；行内粗体/多色混排会被拉平成整行一种样式。图表和地图里的文字会被正常抽出——那本来就是「图里的文字」，属于既定范围。

---

以下是同期一轮独立代码审查的产出：配置落盘、自动更新边界、启动索引自检三处各修掉一个可复现的缺陷，并把三组会跟着开发机状态飘的测试钉死。

### 数据安全

- **设置不再会被一次崩溃清零**：`ui.json` 过去用「先截断再写」落盘，写到一半断电或强杀会留下残文件，解析失败后静默当成"你没设过"——`version_vault_dir`、`index_roots`、版本管理开关一起归零。最坏后果不是"要重设一遍"，而是版本库指向漂回默认位置、程序开一个全新空库继续留底，用户在别处的历史再也接不上。改为 tmp + fsync + `os.replace` 原子替换，并保留一份 `ui.json.bak` 在主文件不可读时回退（且会写日志，不再无声）。
- **并发写设置不再丢键**：全仓 20+ 处配置写入分布在 GUI 线程、FeatureRuntime 线程与后台任务上（如版本管理启动失败时自动关开关）。实测双线程各写各的键，30 轮里 8 轮有一个键被整份覆盖；加进程内写锁后 30 轮 × 3 线程零丢失。
- **自动更新真正只动安装目录**：模块开头写着「只动程序安装目录」的铁律，但没有任何代码在执行它。远端清单里的相对路径原样进入 `staging / rel` 与 helper 的 `Join-Path $dest $rel`，一条 `../../…/Startup/x.bat` 就能落到开机启动项；首轮投毒后本地清单被远端整份替换，下一轮的删除列表同样可控。现在绝对路径、盘符/数据流冒号与任何 `..` 段一律整批拒绝（fail closed），下载、生成 helper 计划两道门各再校验一次；内容寻址块名只接受 sha256 十六进制。

### 性能

- **索引库自检从 O(n²) 降到 O(n)**：`_ensure_filename_index` 的修复分支逐行 `DELETE FROM file_names_fts WHERE file_id=?`，而 `file_id` 是 FTS5 的 UNINDEXED 列，每次删都是全表扫。实测 5k 行 2.3 s / 1w 行 11 s / 2w 行 41 s / 4w 行 156 s（4× 行数 → 67× 耗时），且 `init_db` 是在 `MainWindow.__init__` 里同步跑的，窗口还没显示就已经假死。改为整表一次重建后，4w 行 156 s → 0.24 s，10 万行 0.9 s。这与 `_write_filename_only_batch` 当年修掉的是同一个坑，此处是漏网的另一半。
- **索引格式升级不再白干一遍**：`init_db` 原顺序是「先回填文件名索引、再按 INDEX_VERSION 清空」，撞上格式升级时会把整表回填完再全部删掉。改为版本守门排在最前，4w 行的升级路径 156 s+ → 0.08 s。
- **版本库开库不再重写全表**：`store.init_db` 里那段一次性的 `doc_paths` 迁移无条件全量重跑，真实库（3999 份受管文档）每次开库白付约 45 ms 并把 4000 行重新写脏一遍 WAL。改为只补真正缺登记的文档，判据精确到 `(doc_id, path_key)`，主路径漂移仍会被修；45 ms → 12 ms。

### 测试与工程

- **渲染优先级测试不再依赖"开发机有没有开 PowerPoint"**：v1.0.15 给 `_app_for_render` 加了进程级 ownership 审计后，隔离 fixture 没跟上，于是只要本机开着 PowerPoint 就有 4–5 个用例变红——更糟的是它们其实什么都没验到（渲染在 `_get_app` 之前就被拒了）。fixture 补上 `_powerpoint_process_ids`；模拟"开着"与"没开"两种状态各跑一遍，76 个渲染用例全绿。
- **主题脱钩测试不再依赖 QPA 插件**：offscreen 插件不实现 `setColorScheme`，读回恒为 Unknown，导致任何 headless 跑法稳定变红而原生 Windows 下是绿的。改成运行时探测能力，探不到就只验 palette（与 QPA 无关的主证据仍在）。
- **版本后端启动失败用例不再抢跑**：它盯着「配置是否已落盘」轮询，却断言下一句才发出的回落通知；配置改成原子写后这个窗口从微秒变毫秒，用例随即 5 次里红 4 次。改为等待真正要断言的那个事件，连跑 8 次全绿。
- 清理：删除零引用的 `_relayout_split`、已被分批加载取代的 `_stream_plan_rest` 及其常量与用例；修正 `renderer.shutdown()` 放错位置的 docstring；还原 `main_window.py` 里 15 处被按 GBK 解码过的乱码注释（含一条运行期错误日志文案）；`.gitignore` 补上 `output/` 与 `scratchpad/`。

## v1.3.2 — 2026-08-15

对版本管理做一次独立、故障注入式复审：修复恢复完整性、在线迁移、并发 GC、PowerPoint 占用判断与大媒体内存峰值，并以真实 Office 与长时保存压力测试收口。

### 数据安全

- **损坏当前稿也能恢复**：旧流程先强制给当前文件留底；当前 PPTX 已损坏时，这一步会失败，健康历史版本反而永远救不回来。现在只跳过无法成立的安全留底，恢复点本身仍须通过解析与不可变内容哈希，最后才原子替换目标。
- **全量兜底恢复点不再“拿自己证明自己”**：full 模式过去没有独立的 manifest 哈希，一份被替换成“仍可打开的另一份 PPT”的存档会通过检查。新快照把稳定源的 canonical hash 写入 manifest；旧库的 raw ZIP hash 在回填前也先逐字节验证，发现不一致即隔离，绝不把错误内容正式回填进数据库。
- **恢复点缺件可由健康原稿自愈**：最新版 manifest 或对象缺失时，即使 DB 内容哈希与当前文件相同也不再直接跳过；重新保存会补建一份完整健康版本。
- **恢复不会覆盖正在编辑的 PowerPoint 文稿**：不再用 `open(..., "r+b")` 能否成功来猜测占用——PowerPoint 常允许共享写句柄。现在只读现有 COM 文稿集合，无法完成审计则失败关闭，并在 `os.replace` 前再检查一次，缩小打开文稿的竞态窗口。
- **容量与 GC 的删除顺序加安全门**：容量驱逐在删除第一个健康版本前先做恢复图 dry-run；任一 manifest/对象/分支基异常即整轮中止。驱逐与幽灵收割改为先提交数据库、后删除磁盘 artifact，故障注入中途强杀只会留下可安全回收的孤儿文件，不会再让存活 DB 行指向已消失的恢复点；再次维护会先回收孤儿，再决定是否驱逐健康历史。源文件暂时离线或已被标记删除的文档，在幽灵宽限期真正到期前仍保留最后一个恢复点；`_tmp` 与迁移备份不再误触发健康历史驱逐。

### 迁移与并发

- **版本库迁移改为可回滚的在线切换**：拒绝新旧目录互相嵌套；普通文件逐字节比较，活跃 SQLite 用 backup API 复制并做 `quick_check`；全部验证后才原子封存源目录。新库写/读连接、schema 与设置全部成功后才释放回滚副本，任一步失败都会恢复旧目录和旧连接。
- **“仅切换”立即切换真实连接**：旧设置页只保存新路径，运行中的管理器仍连着旧 DB，随后会出现“元数据写旧库、对象写新库”的裂脑状态。现在新连接先实际读写验证，再同步替换管理器连接与配置。
- **保存、迁移与手动 GC 不再互相踩踏**：稳定副本到 DB/对象提交的完整窗口由位置锁保护；设置页“清理失效版本”不再另开连接绕过管理器锁，避免删除尚未提交引用的在途对象。双线程故障注入验证：GC/迁移都会等待保存完整提交，之后深度审计仍健康。
- **后台循环重启不再复活旧线程**：对账与重维护每一代使用独立 stop Event；旧线程即使超过 join 超时，也不会因新一代清除同一个 Event 而变成永久重复循环。

### 内存与 PowerPoint 共存

- **大媒体全链路流式化**：新快照拆包、恢复重组，以及旧对象池跨盘迁移全部按 1 MiB 块处理，不再用 `ZipFile.read` / `Path.read_bytes` 把 100–500 MB 媒体整块搬进内存。
- **借用用户 PowerPoint 的预览审计失败关闭**：打开历史快照前后同时核对窗口数、文稿数、活动文稿、只读属性、隐藏窗口与精确路径；存在用户文稿却读不到活动文稿时不再按“空会话”继续。
- **压力监控本身不再伪造泄漏**：Windows 探针过去每 10 ms 动态定义一次 `ctypes.Structure`，`ctypes` 永久缓存这些类型，曾制造 500 轮约 300 MB 的假线性增长。探针改为一次初始化复用，并同时记录 RSS、Private Bytes、句柄、线程和 Python 活对象差分。

### 验证

- 500 次、8.1 MB / 40 页、外部进程真实改写 ZIP + 对账/重维护常驻：500 次全部成版，版本数收敛到 100；峰值 RSS +6.8 MB，最终 RSS +3.8 MB、Private Bytes +2.7 MB、句柄 +2；末 10 次/首 10 次耗时比 1.013；版本库为单稿 1.85×；留底中源文件可写，暂存残留 0。
- 3 次 256.1 MB 大媒体保存：单次 3.3–3.6 秒，峰值 RSS +3.1 MB、最终 Private Bytes -0.8 MB，版本库存储比 1.00×。
- 真实 PowerPoint 可见文稿 E2E：真实 Save 触发 watcher、留底中再次 Save、稳定副本不串版、隐藏只读历史预览、用户窗口/活动文稿/Saved 状态保持、只关闭自有快照、打开状态拒绝恢复，16/16 通过；测试创建的 PowerPoint 自行退出，剩余 PID 为空。
- 仓库 E2E：版本管理 7/7、漏事件对账 9/9、版本窗口 12/12、PPT 瘦身与快速 UI 操作压力测试通过；全量 pytest **1246 passed**（含容量驱逐中途强杀与恢复图一致性故障注入）。

## v1.3.1 — 2026-08-15

版本管理专项审查与压测：留底快 2.8 倍、常驻内存封顶、失败会说人话。

### 优化

- **每次保存后的留底快 2.8 倍**：留底会把版本重组成一个临时包做保真自检，而这个包写完就删——此前却对它做了完整 DEFLATE 压缩，等于把几十 MB 的图片再压一遍纯烧 CPU。改成不压缩重组（自检结论不受影响：校验用的是「part 名 → part 内容哈希」的映射，本就与 ZIP 打包方式无关）。**50 MB 稿件实测：自检 1436 ms → 145 ms，整次留底 2.0 s → 0.7 s。** 这是你按下 Ctrl+S 之后 PPT Doctor 要占用的时间。
- **版本库体积统计快 3–10 倍**（承接 v1.2.9）：这条路径在容量上限检查里被持锁反复调用，省下的就是保存被阻塞的时间。

### 修复

- **对象校验缓存不再无限增长**：「这个对象已按哈希验过」的记录只进不出，而对象池实测有 48,775 个文件——托盘常驻越久占用越大。改成有上限的 LRU（淘汰只多花一次重新哈希，不影响正确性）。
- **渲染失败熔断表不再无限增长**：条目按（文件, 页码, 缓存代, 清晰度）计，只在读取时判过期、从不清理，翻过的每一页失败都会永久占一条。现在定期扫掉过期项。
- **恢复失败会说清原因**：最常见的失败是你正开着 PowerPoint 编辑这份稿、目标文件被独占，此前只报一句「恢复失败」。现在会明确告诉你「请先关闭它再试——你的原文件没有被改动」，并区分「恢复点已损坏」「恢复点数据已不在版本库」。
- **版本库的 WAL 不再长期挂着高水位**：WAL 只在超过约 4 MB 时才自动合并，且合并后 SQLite 不会缩小文件，密集留底的长会话里会一直挂着。版本库现在设了回缩上限，由 SQLite 自动处理。索引库刻意不设：它的写入形态是建库时的批量灌入，A/B 实测回缩会让分档写入吞吐的末/首比从 0.84 掉到 0.72，不划算。

### 变更

- **胶片报告「生涯履历」改用 PPT 内嵌的创建时间分年**：此前按文件修改时间，而修改时间会被复制、同步、下载重写——真实库实测 2824 份稿里 2682 份落在同一年，整个「生涯」被压成一格。现在优先读 PPT 包里的 `dcterms:created`，读不到才退回修改时间。**不强制重建索引**：存量文件随各自下一次解析自然补齐。

### 压测（全部临时目录，不碰真实版本库）

新增 `scripts/stress_versioning.py`，200 次连续保存 + 后台线程常驻：

| 指标 | 结果 |
|---|---|
| 内存 | 42.0 → 46.7 MB，中途即持平（无泄漏） |
| 句柄 / 线程 | +3 / 恒定 3（无泄漏） |
| 单次留底耗时 | 首 10 次均值 194 ms，末 10 次 190 ms（无退化） |
| 版本库占用 | 200 个版本的实际内容仅 3.27 MB（稿件本身 3.05 MB，去重生效） |
| 留底期间源文件可写 | ✅ PowerPoint 照常存得了盘 |
| 暂存残留 | 0 |

### 维护

- 主窗口 41 处文档字符串 + 53 处行内注释此前被一次历史编码事故毁成乱码，已按代码实际行为逐条重写（损坏不可逆，不做机械反解）。确认过全程没有一处用户可见文案受影响。

### 验证

- 全量 1224 个测试通过；新增 15 个用例覆盖自检压缩方式、恢复产物仍压缩、缺对象仍能被自检逮到、缓存上限与淘汰、熔断表清扫、恢复失败三种原因、WAL 上限、留底不锁源文件、创建时间读取与回退。

## v1.3.0 — 2026-08-15

自动更新失败不再可能留下起不来的安装；每周版本库重维护不再长时间阻塞保存留底。

### 修复

- **自动更新中途失败会留下混版安装（可能直接起不来）**：更新 helper 逐个文件 `Copy-Item`，某个文件换不动时只记一行日志就继续换下一个，最后照常重启——安装目录里于是同时躺着两个构建的 exe 与 DLL，通常根本无法启动，而用户没有任何退路。现在分三道保险：① **预检**——任一暂存文件缺失（杀毒删除 / 磁盘清理 / 下载残局）就整批放弃，一个字节都不动安装目录，直接重启仍然可用的旧版；② **失败回滚**——替换前先备份原文件，任一文件换不动就把已经换掉的全部还原，安装目录回到一致的旧版，备份目录保留并写入日志；③ **暂存保留**——没换成功就不删暂存，下次可续用而不必重下整包。
- **每周版本库重维护不再长时间阻塞保存留底**：重维护此前全程持有版本管理锁，而其中两件最慢的活都是只读的——幽灵探测要对上千条登记路径做 `os.path.exists`（而且「补记缺失」和「收割」各跑了一遍，等于白探两次），版本库体积统计是整目录遍历。真机 3999 份文档 / 3.43 GB 库上合计约 10 秒，期间每一次 PPT 保存的留底都在排队。现在两件事都挪到锁外、且只探一次分给两处用；真正删除前仍逐个复核路径是否复活，宁可白探一次也不误删。

### 验证

- 全量 1209 个测试通过；新增更新器预检 / 回滚（真实驱动 PowerShell helper）、幽灵探测复用与复核、主窗盘点接线与上限等 9 个用例。
- 版本管理三套 E2E 全绿：补录对账 9/9、端到端版本管理 7/7、PPT 瘦身。

## v1.2.9 — 2026-08-15

拿真机数据校准「索引所有文件」：默认改回关闭，开启后补上实时通道；版本库迁移补上真正的字节校验。

### 修复

- **版本库迁移会在内容被截断时删掉源目录（数据丢失）**：迁移函数的说明写的是「复制 → 校验（文件数+字节）→ 删除源」，但字节校验从来没有接线，实际只比了文件数就 `rmtree` 源目录。目标盘写满、网络/移动盘中途截断都会留下「个数对、内容短」的文件——而对象池是内容寻址的，短文件要等用户真正回滚时才暴露成「恢复点损坏」，那时源已经没了。现在逐文件比对字节，`versions.db`（走 sqlite backup，体积本就与源不同）改为结构自检；任一不符即回滚目标目录并保留源。
- **「任意文件名」现在有实时通道**：watcher 此前只对 PPT / Word / PDF 做实时索引，其它类型的新建与删除要等下一次完整扫描（最坏一周）才反映到搜索结果。现在 watcher 只上报「有非内容文件变动的目录」，后台每分钟按目录 `scandir` 增量对账——成本与事件数无关，只与目录内条目数有关，浏览器缓存那种一分钟几千次 churn 会被折叠成一个目录名。新建与删除的文件约一分钟内进出搜索结果。

### 变更

- **「索引所有文件」默认改回关闭**。真机实测：固定盘共 **202 万个文件**，其中 PPT / Word / PDF 只有 **1934 个**——1000:1。噪音的大头是 `.py` 39.9 万、`.h` 15.8 万、`.png` 15.8 万这类源码树产物；为它们把索引库从 138 MB 撑到约 500 MB、首次启动多花数分钟，对「PPT 小助教」的默认体验是净亏。功能完整保留，设置 → 功能里一键可开，与 v1.0.12 立下的「高阶能力用户主动选择」原则一致。
- 从 v1.2.8 升级、且没有手动开过这个开关的用户：盘点数据会在后台自动清理，**不会**因此触发一次全盘重扫（内容索引口径没变，没有理由让用户白等）。

### 新增

- **库体检新增「版本库占用」卡片**：直接摆出版本库当前占用、受管文档数，以及「源文件已经不在磁盘上」的失效留底份数与可回收估计。此前这些数字只藏在设置页，用户得先点「清理失效版本」才知道有多少——而真机实测 3999 份受管文档里有 **1924 份**源文件已经没了，正是「C 盘暴涨」反馈的直接来源。

### 优化

- **版本库体积统计快 3–10 倍**：`vault_size_bytes` 从 `Path.rglob`（每个条目额外两次系统调用）改为 `os.scandir` 递归（Windows 上直接复用目录枚举带回的元数据）。真实 3.43 GB 版本库实测 **7.13 s → 2.12 s（冷）/ 0.6 s（热）**。这条路径在每周重维护里被容量上限检查全程持锁反复调用，省下的就是保存 PPT 时留底被阻塞的时间。

### 验证

- 全量 1202 个测试通过；新增 16 个用例覆盖目录级对账（新增/删除/非递归/排除目录/churn 合并/内容行不可降级）、默认值回退与签名豁免、迁移字节校验回滚。

## v1.2.8 — 2026-08-15

「任意文件名搜索 / 生涯履历 / 界面字体 / 自定义索引根」四项新能力首次发布，并修掉深度复审中暴露的两个会伤数据的缺陷。

### 新增

- **任意文件名搜索（Everything 式）**：全盘登记所有文件的名字（不解析内容），搜索模式里新增「任意文件名」。默认开启，可在设置 → 功能里关闭（关闭后停止收录并后台清理盘点数据）。非 PPT / Word / PDF 的文件不走实时监听，新建与删除最坏要等一周的完整扫描才反映到结果里，已在设置项说明中注明。
- **胶片报告新增「生涯履历」Tab**：按年输出当年份数 / 页数 / 字数 / 体积、时光机留版数、年度关键词 Top5 与代表文件（带「定位」）。年度关键词按年分层抽样，早年不会被近年挤占；版本库未连接时留版数显示「—」而不是 0。
- **界面字体可调**：设置 → 功能新增字族与字号档（90% / 100% / 115%），点「应用」即时全 App 生效。
- **自定义索引根**：设置 → 功能新增索引范围编辑器，可添加本地目录或网络路径（`\\server\share\dir`）。自定义根与本地固定盘一起被索引（增量语义，不是替换）；网络路径当前不可达也可保存，可达时自动纳入。

### 修复

- **Unicode 分词**：希腊字母（τ λ Δ）、带音标拉丁（é ü ñ）、假名、韩文、CJK 扩展 A 汉字、非 ASCII 数字此前被当成分隔符整条丢弃，根本搜不到。分隔符判定统一为唯一事实源，索引与查询两侧共用。INDEX_VERSION 升 7，首次启动会自动重建一次索引。
- **docx / PDF 内容索引可能被永久冻结（数据正确性）**：「开启文档搜索 → 索引 → 关闭文档搜索 → 编辑该文件 → 重新开启文档搜索」这条路径下，文件名盘点会把新的 size/mtime 刷进内容行，而 size/mtime 正是判断「内容要不要重新解析」的唯一信号——结果重新开启后直接跳过解析，搜出来永远是旧文字、旧页码，且不会自愈。现在盘点完全不碰已有内容行。
- **版本库容量上限不再删掉活文档的最后一个版本**：超出上限时的「从老到新驱逐」原先没有任何每文档保底，而生产库实测 3800 份文档里 3700 份只有 1 个版本——一次超限驱逐等于按「最久没动」的顺序逐份抹掉整份文档的全部回滚历史，而那恰恰是最需要旧版的一类。现在每个源文件仍在磁盘上的文档至少保留最新 1 个版本（分支基与隔离版本的豁免不变）。
- **版本库重维护不再被 VACUUM 失败卡死**：VACUUM 需要约两倍库体积的临时空间，磁盘紧张时会稳定失败，此前会导致 7 天节流标记永远写不进去，于是每次启动都重跑一整套幽灵扫描 + 驱逐 + GC（全程持锁）。VACUUM 现在归为可选步骤，失败不挡节流。

### 优化

- **百万行索引库的界面刷新提速**：开启文件名盘点后 `files` 表可达百万行，而状态栏 / 仪表盘 / 启动对账的热查询全是 `lower(ext)` 与 `status` 上的过滤与分组，此前每次都是全表扫。新增表达式索引后 100 万行实测：库统计 853 ms → 1.5 ms、类型分布 695 ms → 133 ms；代价是盘点写入 +41%、库体积 +19%。

### 验证

- 全量 1186 个测试通过；新增每文档保底、活文档最后一版、已删文档可全驱逐、开关横跳端到端等用例。

## v1.2.7 — 2026-07-30

版本库减负三件套：存储位置可选、暂存随库、失效版本清理（用户实测反馈 C 盘 vault 暴涨）。

### 新增

- 版本库存储位置可选：设置页版本管理区新增存储位置选择（默认不变）。已有数据时可选择整体迁移到新位置（复制 → 校验 → 删除源，带进度）或仅切换；校验会拦截系统目录、盘符根目录与不可写位置。开启版本管理不再意味着 C 盘必涨。
- 清理失效版本：设置页新增「清理失效版本（源文件已删除的留底）」。源文件已删除/移动的文档留底（实测可占版本库近一半）先报数、确认后删除并回收对象池空间。

### 修复

- 留底暂存随版本库位置：留底过程的完整文件暂存副本从系统 Temp 改到版本库 _tmp 目录——版本库迁出 C 盘后，大文件副本也不再过手 C 盘；启动清扫同时覆盖两处。

### 验证

- 全量 1024 个测试通过 + 1 跳过；新增 7 个用例（位置选项/校验/暂存随库/清扫/收割/迁移）。

## v1.2.6 — 2026-07-30

修复版本留底暂存文件泄漏撑爆 C 盘的问题（用户实测反馈）。

### 修复

- 版本留底暂存泄漏：开启版本管理后，每次留底都会在系统 Temp 目录暂存一份完整文件副本（大文件可达数百 MB）；此前暂存删除失败会被静默忽略（杀软/索引器短暂锁定新写入的大文件时即残留），进程被杀也会残留，且没有任何清扫机制——残留文件跨天累积，可把 C 盘堆满。现：应用启动时自动清扫全部历史残留暂存（未开启版本管理的用户同样受益）；暂存删除改为退避重试，最终失败会记录警告不再静默。
- 临时缓解（本版之前的版本）：手动删除 `%TEMP%` 下 `.pptdoctor-snapshot-*` 文件即可，均为留底暂存废品。

### 验证

- 全量 1017 个测试通过 + 1 跳过；新增 5 个暂存清扫用例。

## v1.2.5 — 2026-07-24

修复界面主题被本机 Windows 深浅模式渗透的问题。

### 修复

- 主题与本机深浅模式脱钩：Qt 6.5+ 起 Windows 默认跟随系统深浅模式，应用此前只设全局样式表，未被样式表覆盖的控件（对话框、通用输入框、弹窗等）会回退到本机系统调色板——本机深色主题下出现"黑框内文字看不清"。现按应用主题明暗显式钉住 colorScheme、并用应用主题色生成全局调色板作为未覆盖控件的回退，系统深浅切换不再影响应用界面；12 套主题全部验证通过。

### 测试

- 渲染测试套件隔离本机真实 PowerPoint：后台渲染安全闸在本机开着 PowerPoint 时拒绝渲染（保护用户文稿的正常功能），此前 5 个用例未做环境隔离会误失败。

### 验证

- 全量 981 个测试通过 + 1 跳过（本机 PowerPoint 开启状态下）；新增主题脱钩对比度用例覆盖全部 12 套主题。

## v1.2.4 — 2026-07-24

从「下游节流」推进到「源头抑制」：监听/索引范围收敛 + 预览状态机 + 包体瘦身。

### 修复

- 源头门禁：无变化的文件事件（OneDrive churn、构建工具 touch 等）在索引入口即返回，不再重复解析、写库、触发状态统计与重搜——v1.2.3 的 15 秒节流只掐重搜，churn 事件仍全量解析。same-stat 判定由全量扫描与 live 索引共享，解析错误重试、云占位水合、内容 hash 回填语义完整保留。
- 尾随刷新：节流窗口内的最后一次文件变更不再丢失（dirty latch + 按剩余时间重排，不再依赖"也许还会来"的下一次事件）；空查询仪表盘与搜索页共享 15 秒后台刷新预算；手动搜索消费待刷新标志，不再出现手动搜完 2.5 秒后又自动重搜一次。
- 预览状态机：成功/在途去重 key 分离，渲染失败或取消后同页可正常重试；修复翻页渲染失败后翻回上一页被永久去重、错误提示画面残留的回归。
- 历史下拉：打字时不再自动弹出历史抢 Enter/Esc/方向键（Enter 曾会把查询词改写成历史项）；仅鼠标点击空框 / Ctrl+L / 全局热键显式唤起，选中即写回、关闭即卸载。
- 项目构建产物排除：识别软件项目（package.json / pyproject.toml / .git 等标记）下的 dist / build / out / target / artifacts 构建产物目录，在扫描、文件监听全事件、live 索引、版本快照四层统一拦截；普通业务同名文件夹（如 Documents\dist）不受影响；显式把此类目录加为库根时四层一致尊重纳管；既有构建目录索引随扫描策略升级自动清退。
- 更新器测试日志隔离：helper 日志路径可注入，集成测试不再往生产 update.log 写假"swap done"记录。

### 优化

- 包体：裁剪 jieba 未使用的关键词/POS/LAC 模型与 Qt 侧运行时加载的 OpenSSL 冗余项（应用 QtNetwork 仅用于本地单实例 IPC；Python HTTPS 保留非后缀 DLL 对并有冻结自检护栏）。解压目录 119.31 → 89.98 MiB（-24.6%），ZIP 56.55 → 39.19 MiB（-30.7%）。

### 验证

- 全量 980 个测试通过；冻结包自检 12/12（OpenCC 繁简、jieba 切词、SSL、ProcessPool）；冻结 E2E 通过（包清单、单实例 IPC、root-scoped watcher 端到端快照）；真实库 3232 文件索引实测。

## v1.2.3 — 2026-07-21

修复 OneDrive 等持续同步环境下的「一直在搜索 + CPU 高」。

### 修复

- 后台重搜节流：文件监听（watcher）在 OneDrive 持续 churn 时不断触发 live 索引，此前每个 2.5 秒安静窗口都会对当前查询词做一次全量重搜，churn 不停重搜不止。现后台重搜最小间隔 15 秒，且用户刚输入的 5 秒内不抢跑；手动搜索、筛选、排序永不节流。
- 预览渲染去重：同一文件同一页（mtime 未变）不再重复发起 PowerPoint COM 渲染，消除重搜连带取消/重发渲染造成的 CPU 搅动；翻页、换文件、文件被编辑后照常渲染。
- 选中未变时不再重复加载版本时间线与大纲（内存缓存回填，切换文件后照常加载）。

### 根因说明

- 搜索逻辑本身无死循环（真实库 3232 文件实证单次搜索 0.55 秒）；问题在「外部文件 churn × 每次重搜的全量连带动作」，已按节流 + 去重治理。

### 验证

- 全量 953 个测试通过；新增 6 个用例覆盖节流窗口、输入宽限、渲染去重、detail 缓存。

## v1.2.2 — 2026-07-20

修复全局热键唤起「一闪而过」。

### 修复

- Alt+F / 托盘唤起后窗口稳定停在前台，不再闪一下消失：一次按压的热键消息可能被投递多次（含长按自动重复），旧切换逻辑在一次按压里连续翻转「显示→隐藏」；现按 0.4 秒去抖合并为一次切换。
- 前台获取更稳：新增 AttachThreadInput + 短暂 TOPMOST 脉冲的 Win32 唤起路径，Windows 前台锁定严格时也能把窗口带到最前（此前仅 raise + activateWindow，被拒时只闪任务栏）。
- 已在真实桌面做红绿验证：修复前单击 Alt+F 窗口最终隐藏（复现一闪而过），修复后单击 / 长按均稳定前台。

### 验证

- 全量 945 个测试通过；真机注入式验证 3 组（旧代码复现失败 → 新代码单击 / 长按均前台稳定）。

## v1.2.1 — 2026-07-19

体验修复轮：搜索框不再被按钮"唤醒"，选中不再被后台索引拽走，体检与状态提示更好用。

### 修复

- 搜索历史下拉不再自动弹出：此前点击导航轨、主题菜单等按钮后，焦点经窗口重新激活 / 浮层关闭归还回到搜索框即弹出历史列表；现在只有真实点击搜索框本体才弹出，Ctrl+L 与全局热键 Alt+F 的显式唤起行为不变。
- 选中与预览不再被拽回旧文件：live 索引后台重搜后选中项按文件路径保留（不再无条件跳回首条；用户主动改词仍回首条）；选中切换即刻作废旧渲染请求，迟到的上一文件渲染结果不再上屏。
- 「N 个文件夹无权限」状态提示可点击：弹出完整路径列表（此前只存计数与前 5 条示例；索引器现记录全量，上限 500 条防爆），支持复制全部路径与一键打开健康诊断。

### 增强

- 库体检新增「全部回收」：不再受前 40 组展示上限影响，一次回收全部重复副本；回收分块执行并显示确定态进度条；送系统回收站可还原。
- 体检报告条目可点击：僵尸冷文件、终版诅咒、体积巨无霸、解析失败与重复组文件名均可点击，跳到搜索页按文件名定位。

### 验证

- 全量 945 个测试通过；新增 17 个用例覆盖焦点原因、选中保留、迟到渲染丢弃、回收分块与进度、链接跳转、无权限列表。

## v1.2.0 — 2026-07-19

单窗工作台：导航轨把 7 个窗口 / 浮层容器收进 1 个窗口；筛选、详情、版本、报告各就其位。

### 左侧导航轨：一个窗口装下全部

- 新增 56px 导航轨：搜索 / 概览 / 版本 / 健康四个主页面 + 底部报告 / 设置入口；关掉主窗不再有"窗口还藏在某处"的错觉。
- 概览（库仪表盘）从结果列表的客串位提升为独立全宽页；版本管理、库体检从独立窗口改为懒加载内嵌页，首次切入才构造加载。
- 搜索框全局常驻：任意页面输入搜索词自动切回搜索页；清空搜索词只在搜索页时回落概览，在版本 / 健康页不强行拽走。
- 托盘「版本管理…」独立窗口作为权力路径保留，与内嵌页并存。

### 合一工具栏再瘦身

- 设置、胶片报告两枚图标退出工具栏，入口统一收进导航轨；工具栏只剩品牌、搜索、类型 / 范围下拉、主题与窗口控制。
- 状态栏数字点击彩蛋一并收编，报告入口唯一化。

### facet 筛选：从抽屉到条件行

- 筛选面板不再占据 splitter 左栏，结果列表整体变宽约 8%。
- 列表顶栏左侧新增条件区：已选条件以 chip 平铺、点击 ✕ 即移除；「+ 筛选」原位弹出筛选面板浮层；四维桶与筛选语义完全不变。

### 预览卡 Tab 化：预览 / 大纲 / 版本 / 详情

- 浮动 DetailPanel 弹窗退役，大纲、版本时间线、文件详情收进预览卡下方四个 Tab，选中文件即加载，无需再开第二个窗口。
- 「有新版本」红点从详情按钮迁到「版本」Tab 标题；PPT 瘦身、恢复 / 导出、版本缩略图预览入口全部保留。
- 新手聚光灯引导重新锚定到 Tab 条。

### 胶片报告跟随主题

- 报告浮层不再是固定深色 FILM 第三套语言：配色随当前主题（静白 / 静黑及 10 套玻璃主题均生效），FILM 降为缺键回退源。
- 导出图片 = 所见即所得；热力图火焰色阶与峰值描边作为报告品牌色保留。

### 状态栏：上次索引时间常驻

- 状态文本右侧新增「上次索引 HH:mm」（非当天显示 MM-dd HH:mm）；语义为上次全量索引完成时间，实时增量索引持续进行。

### 验证

- 全量 928 个测试通过；新增导航轨页面仲裁、条件 chip、Tab 结构、主题化报告明暗差异等 9 个用例。
- 离屏截图覆盖概览 / 搜索 / 版本 Tab / 健康页 / 主题化报告 / 静黑六视角验收。

## v1.1.0 — 2026-07-16

界面全面换代为「静白工作室」设计语言：像成熟桌面软件一样安静、精确，信息主次分明。

### 新默认主题：静白 / 静黑

- 不透明纯净层级取代半透明玻璃与极光光团；深色态背景不再发灰发脏，文字对比稳定。
- 旧 10 套极光玻璃主题全部保留，可在主题菜单随时切回；已选旧主题的用户不受影响。
- 强调色预算制：整屏蓝色只允许出现在选中指示条、主按钮与焦点环；命中词高亮改为荧光笔黄。

### 布局与控件

- 标题栏与搜索栏合一：品牌、搜索框、类型 / 范围下拉、图标按钮、窗口控制同居一行，顶部省出一整行高度；标题栏空白处仍可拖动窗口，交互控件处不误触拖拽。
- 筛选 / 设置 / 主题 / 胶片报告四个文字按钮改为 32px 图标按钮，悬停出完整说明。
- 结果卡片：命中页码合并为按页码排序的等宽灰标签；匹配类型徽章灰化；文件名悬停显示全名；修改时间等宽对齐。
- 预览区：路径面包屑化（目录淡化、文件名加粗，悬停看完整路径）；页码按钮变分段控件并按页码排序；头部动作收敛为「复制本页文字 / 复制路径 / 详情」一行；底部仅保留一个实心主按钮。
- 渲染失败文案从「COM 原图渲染失败」改为「暂时无法生成原图预览」+ 可执行的重试指引；主按钮不可用时正确置灰。

### 一致性细节

- 控件圆角三档制（卡片 12 / 控件 8 / 小件 5），hover 只变底色，Tab 焦点环全局统一为一根强调色描边。
- 数字、路径、耗时、热键统一等宽字体；状态栏热键标签键帽化。

## v1.0.15 — 2026-07-16

本版解决 v1.0.14 的核心可用性缺口：用户正在使用 PowerPoint 时，PPT Doctor 不再直接拒绝预览；同时不冒充“多实例隔离”，而是用可审计的隐藏只读快照安全共用单实例 COM。

### 用户 PowerPoint 打开时也能看 COM 原图

- 没有 PowerPoint 时，继续创建一个隐藏实例，并以新 PID、COM/DcomLaunch 创建父进程、精确进程句柄、无可见窗口四重证据确认它属于 PPT Doctor；用户恰好同时手动启动 Office 时不会把该 PID 误认成自有进程。
- 用户 PowerPoint 已打开时，前台预览只借用其 `Application` 引用，实际打开的是缓存目录中的 `ReadOnly=1, WithWindow=0` 快照；不碰原文件，不修改 `Visible`、活动文稿、显示设置或保存状态。
- 借用后强制审计：快照必须只读、窗口数为 0、路径精确匹配，用户窗口总数和活动文稿必须保持不变；任一条件不能证明即关闭快照并失败，不扩大权限。
- 清理只关闭本工具持有的快照。借用模式永不调用 `Application.Quit`、永不终止用户 PowerPoint PID；临时关闭被 COM 拒绝时保留精确对象引用继续重试，避免随机名文稿遗留任务栏。
- 失败提示不再错误要求用户关闭 PowerPoint；若 Office 正忙或有弹窗，只需完成当前操作后重试。

### 单通道更快，而不是堆 COM 客户端

- Microsoft 将 PowerPoint 定义为 Multiuse（Single Instance）COM 服务；本机反复实测 `DispatchEx` 的 2–3 个客户端始终共用一个 `POWERPNT.EXE`。
- 固定导出 12 页时，单客户端约 628 ms；双客户端约 721 ms；三客户端复测约 668 ms、另一轮约 866 ms。多客户端没有吞吐收益，反而增加争用、CPU 和内存，因此继续保持一个串行 COM 通道。
- 用户 PowerPoint 已打开时，真实产品路径首张 1600px 预览约 251–315 ms；复用同一隐藏快照导出相邻页约 49–116 ms。渲染前后用户窗口、活动文稿和 Saved 状态均不变。
- 空闲 350 ms 后仍完整释放快照和 COM，但保留隔离渲染服务进程阻塞在本地 socket 上。实测其空闲 CPU 为 0%、RSS 约 29 MB；下一次预览免去子进程重启与握手，约节省 262–615 ms。
- 没有用户 PowerPoint 时，冷启动仍由 Office 自身主导（本机约 3.3 秒）；第二次在复用渲染服务进程后约 2.7 秒。每次空闲释放后隐藏 `POWERPNT.EXE` 都会退出，不以长驻 Office 换速度。

### 验证

- 新增前台借用授权、后台禁借、只读无窗口审计、活动文稿不变、所有权降级、IPC 参数透传、COM 释放但子进程复用、最终退出等红绿回归。
- 真机 E2E 同时覆盖“用户可见文稿 + 产品直连渲染”“用户可见文稿 + IPC 渲染”“无用户 PowerPoint + 自有隐藏实例”三条路径；每轮结束均确认无多余窗口、无哈希文稿、无 PowerPoint / 渲染子进程残留。

## v1.0.14 — 2026-07-15

本版纠正 v1.0.13 把“可读内容”当成“页面预览”的产品偏差：右侧预览重新收敛为单一 PowerPoint COM 原图链路，不再提供文字、封面、Shell、PDF 或第三方渲染降级。

### COM 原图成为唯一结果

- 未命中合格缓存时，界面只显示“正在启动 / 等待 PowerPoint COM 原图渲染”；成功后显示 COM 直接导出的页面，失败则明确显示“COM 原图渲染失败”。
- 删除命中页文字速览 API、并行安全预览任务、LibreOffice 转 PDF、QtPdf 光栅化以及“带预览引擎完整版”打包路线，减少额外 CPU、进程和模糊的产品语义。
- 右侧只接受达到当前显示需求的 1920–2560 长边 COM PNG；不再把 960px 缓存放大成临时预览。相邻三页仍由同一个串行 COM 会话以 1920px 预取，翻页不增加并发。
- 缓存键加入 `com-only-v1` 代际，v1.0.13 可能由非 COM 路线生成的图片不会再被读取；缓存维护会主动清理文字速览、兼容 PDF、临时工作区与旧 profile。
- 若用户 PowerPoint 已在运行，仍拒绝把哈希快照塞入其单实例会话；此时明确失败并提示关闭 PowerPoint 后重试，绝不以降级图掩盖失败，也不重演随机文件名与显示状态污染。

### 任务栏图标

- EXE、Qt 窗口、托盘和桌面快捷方式统一使用同一份多尺寸 `app.ico`；快捷方式直接读取 EXE 第 0 个图标资源，并移除与普通 `.lnk` 不一致的自定义 AppUserModelID。

### 验证

- 新增严格 COM-only 红测：活动 PowerPoint 场景不得调用任何非 COM 路线、缓存代际必须与旧图隔离、文字预览 API 必须不存在。
- 冻结包不再携带 `Qt6Pdf.dll` / `QtPdf.pyd`，分发体积和运行依赖同步收口。

## v1.0.13 — 2026-07-15

本版继续收口“后台工作存在，但不能抢走前台体验”。重点优化全盘补漏时的状态呈现，以及用户已经打开 PowerPoint 时的预览安全与翻页速度。

### 扫描状态更安静

- 全盘扫描不再把长路径和持续变化的文件名塞满状态栏；界面只保留“扫描 / 建库”、发现或完成数量、百分比与预计剩余时间，详细路径移到悬浮提示。
- 新进度条只在真正扫描且可见时以 20 FPS 粗粒度动画；确定进度、隐藏窗口和任务结束后立即停表，减少无意义重绘与视觉噪声。
- 搜索进行中会继续保留搜索状态，不被后台建库文案覆盖；用户能清楚区分“我正在搜”与“软件空闲补库”。

### PowerPoint 已打开时也不污染会话

- 预览仍然禁止借用用户正在运行的 PowerPoint；即使能精确定位窗口，也不在其中打开临时快照，根除 `Close` 短暂失败后随机名演示文稿滞留任务栏的风险。
- 第 1 页优先读取 PPT 内嵌封面；任意后续命中页可在不启动 Office 的情况下生成明确标注的“文字速览”，只解析被点中的单页。高清图稍后安全替换，高清失败时速览仍保留，不再把核心能力直接降级成“无法预览”。
- 文字速览改走独立的“只留最新请求”交互通道，不再被前一个文件最长 30 秒的高清转换堵住；请求编号与高清完成标记双重防护，旧页或迟到的速览不会覆盖当前高清图。
- 旧版 `.ppt`、加密、结构异常或索引页码过期时，也会显示明确的安全说明图，不再只留空白失败页；原文件不会因此被读取写回或改动。
- 若本机有 LibreOffice，用户明确点击预览且 PowerPoint 正忙时，会用独立低优先级进程把安全副本一次性转为缓存 PDF；首次转换后，后续翻页只在本地渲染缓存页，不接触 PowerPoint。
- 修复 LibreOffice 已成功产出 PDF、但退出尾声临时目录仍被占用时被误判为“预览失败”的收尾竞态；清理失败不再抹掉已经成功的首帧结果。
- 兼容 PDF 纳入 2GB / 2000 项统一 LRU 上限，残留工作目录 10 分钟后回收；转换超时只终止命令行带本次随机隔离 profile 的 LibreOffice 进程，不会误杀用户自己的 LibreOffice。
- LibreOffice 兜底不用于后台预取，最多等待 30 秒且按文件内容缓存；缺少 LibreOffice 时仍保留内嵌封面、既有缓存或命中页文字速览。

### 验证

- 新增用户 PowerPoint 会话禁借、任意命中页速览、旧格式说明页、迟到速览防覆盖、LibreOffice 一次转换多页复用、缓存回收、临时目录清理竞态、精确超时进程回收等回归测试；冻结版自检真实执行 QtPdf 的 PDF→PNG 路径。
- 本机最终代码实测：未缓存的命中页文字速览约 442 毫秒；独立高清转换首次约 12.2 秒，缓存后翻到第 2 页约 100 毫秒；转换结束无残留 LibreOffice / PowerPoint 进程。
- 最终源码门禁 `899 passed, 2 deselected`、退出码 0；冻结版内容搜索自检除 `12/12` 搜索用例外，额外真实验证 QtPdf 的 PDF→PNG 路径，冻结 watcher 改存留版验证通过。

## v1.0.12 — 2026-07-15

本版把 v1.0.11 的六大主题胶片报告与新一轮深度流畅度治理合并交付。目标不是让后台任务跑得更快，而是让搜索、排序、翻页、打开文件始终优先，空闲时再低负载补齐全盘索引与版本保护。

### 默认轻量、按需增强

- 新用户默认进入基础模式：只开启 PPT 搜索与统计；PPT 自动版本管理、Word/PDF 搜索和智能分组均可在设置中独立开启，避免未使用的能力常驻消耗 CPU、I/O 与内存。
- 功能开关与数据状态分离：关闭增强能力不会删除已有索引或历史版本；重新开启后可继续使用，降低试用成本与误操作风险。
- 全盘补漏仍覆盖固定磁盘以及 `AppData\Roaming` 等企业软件目录，但自动扫描固定为低优先级、最多两个解析进程；检测到搜索、翻页、打开/恢复文件等用户交互时暂停继续派发后台任务。
- 扫描被取消、超时或磁盘暂时不可用时，不再把本轮未看到的文件当成已删除，防止不完整枚举污染索引。

### 搜索与界面不再互相拖累

- 搜索数据库改为只读、短超时连接；写库繁忙时快速返回并由界面合并重试，不再让“正在搜索”长期挂住。全文结果仍保留原文验证、完整短语优先、大小写开关和主/次排序组合。
- 排序、筛选、版本列表、胶片报告等文件与数据库读取全部移出 GUI 主线程；后台任务完成或取消后立即退役，修复长时间使用中线程对象与回调逐步堆积的问题。
- 搜索结果继续采用纯文本卡片，不生成封面缩略图；渲染资源集中给用户选中的右侧大图和实际翻页。
- 后台任务池为打开、恢复、导出等显式操作保留通道；体检、统计、扫描不能占满所有执行槽。

### PowerPoint 预览与安全边界

- 预览继续运行在隔离渲染进程中，并加强自有 PowerPoint PID、窗口句柄和临时演示文稿的有界清理；只处理本工具创建的隐藏实例，绝不关闭用户已有 PowerPoint。
- 翻页采用方向感知的三页小窗口缓存：当前页优先，随后只在同一安全会话空闲时预取前后相邻页；连续翻页会淘汰过时请求，不额外并发启动 PowerPoint。
- 渲染缓存按文件版本与目标清晰度复用；已有可用图先显示，再在用户停留后补高清，避免重复导出同一页。
- 版本恢复/导出若后端提交失败，界面会回滚忙碌状态并明确报错，不再留下按钮永久禁用或“处理中”假状态。

### 验证

- 新增基础/增强模式、低优先级全盘扫描、只读搜索退避、渲染 IPC 与缓存、任务退役、版本操作回滚等回归测试。
- 独立长时压力测试覆盖持续搜索、排序、预览与空闲观测；发布包继续执行真实冻结环境的 12 项内容搜索自检。
- 合并后交叉回归 `474 passed`，完整测试门禁 `881 passed`；两组均为零失败退出。

## v1.0.11 — 2026-07-15

本版基于 v1.0.10 重做“我的胶片报告”：把用户确认的 12 项主统计与 25 项趣味统计全部落地，并用主题 Tab 控制信息密度与计算负担。

### 六类主题报告

- 新增“总览 / 名人堂 / 创作节奏 / 版本时光机 / 内容人格 / 片库版图”六个键盘可切换 Tab；不再把几十张卡片堆成一条长页面。
- 加入 PPT 之最、文件名长短与高频词、最老/最新、同名双胞胎、目录深度、一日爆肝、常见页数、默认名俱乐部、标点人格、会议时长与纸张高度等趣味统计。
- 加入口头禅、主题星座、开场/收尾仪式、重复句、数字/英文/问号人格、关键词月度趋势，并明确显示本地样本范围。
- 加入创作年轮、四季旺季、今日回忆、周年纪念、成就徽章和确定性的“片库一句话”。

### 真正的版本时光机统计

- “最能改奖”改读 `versions.db` 的真实健康快照，不再把近似内容分组当成版本次数。
- 加入真实保存时钟、单份 PPT 成长史、最大单次改稿、年度最大改稿夜、72 小时改稿冲刺、已删除可恢复、越改越长/越改越瘦、沉睡复活、改名/迁徙和页数反复横跳。
- 版本库始终只读打开；报告不重建历史 PPT、不读取对象池，也不启动 PowerPoint。

### 性能与可靠性

- 文件/路径统计只查索引库；内容分析固定最多 2,500 页、40 万字，并在报告后台任务中完成。生产库 2,730 份 PPT、4,076 个版本点下，首次生成由 6.24 秒压到 1.91 秒。
- 同一报告浮层按范围缓存；切 Tab 只重建当前页 UI，不重复统计。复制和导出只处理当前 Tab，避免生成超长大图。
- 修复 Tab 快切时旧组件延迟销毁造成的一帧叠影；未知 `mtime=0` 不再冒充 1970 年的“最老 PPT”。
- 完整门禁：811 passed、1 skipped；真实生产库完成六个 Tab 的离屏视觉检查。

## v1.0.10 — 2026-07-14

本版继续围绕“任何后台工作都不能绑架用户操作”做深度收口，重点修复渲染阻塞、退出残留、后台任务饥饿和大版本库筛选卡顿。

### PowerPoint 打开与退出

- 渲染 IPC 增加独立于请求锁的紧急中断通道：用户打开原稿或退出时，可以直接关闭本工具的 socket 与隔离子进程，不再被最长 20 秒的 COM 请求锁反向卡住。
- 打开原稿先给预览 750 ms 收尾；仍未释放则中断隔离渲染器，并等待隐藏 `POWERPNT.EXE` 消失。若无法确认安全交接，拒绝打开并提示重试，绝不把原稿交给低清预览会话。
- 主窗口退出移除 `QThread.terminate()`；渲染线程采用 stop → abort → bounded wait，更新线程通过关闭活动 HTTP response 唤醒阻塞读，让 finally 清理可以正常执行。
- 历史版本预览新增 IPC `render_once`：渲染、关闭临时演示文稿在同一子进程请求内完成，不再调用全局 renderer shutdown 干扰当前预览。

### 交互优先与低负载

- 通用后台任务池为打开、恢复、导出等显式用户操作保留一个通道；普通诊断、历史查询最多占其余通道，避免按钮点击长期排队。
- 等待执行的后台任务支持取消，主窗口退出会先停止所有队列任务，再做有界等待。
- 渲染诊断改为非阻塞读取；渲染正忙时返回 `busy` 状态，不在 GUI 线程等待请求锁。
- 自动全盘补漏同时降低索引线程与解析子进程的 Windows CPU/I/O 优先级，并固定单解析进程。
- 修复自动单 worker 扫描走 inline、绕过 60 秒解析超时的问题；隔离进程不可用时自动任务安全中止，不回退到不可终止的线程池。
- 版本管理文档列表超过一批时每次只创建 160 个 Qt item，通过零延迟事件循环分批补齐；新筛选会使旧批次立即失效。

### 验证

- 新增渲染紧急中断、隐藏 PowerPoint 安全交接、无强杀退出、交互保留通道、后台扫描隔离、版本列表分批和更新连接取消等回归测试。
- 完整测试门禁：802 passed、1 skipped；冻结版自检随发布包再次执行。

## v1.0.9 — 2026-07-14

本版修复预览自动化会话泄漏到用户 PowerPoint、导致画面持续模糊并在任务栏出现哈希文件名快照的核心缺陷，同时完成全盘补漏、结果卡降载、翻页快切和胶片报告增强。

### PowerPoint 会话隔离

- 根因确认：旧“打开并跳页”直接用 COM 打开原稿，可能复用预览渲染器创建的隐藏 PowerPoint 进程；工具快照与用户原稿因此共处一个自动化会话，退出 PPT Doctor 后该 PowerPoint 进程仍会继续影响显示。
- 打开 PPT 前先在渲染线程完整释放预览会话；随后只由 Windows 文件关联正常打开原稿。跳页改为只附着路径完全一致的已打开文档，绝不再调用 `Dispatch` 或 `Presentations.Open` 打开用户原文件。
- 预览创建 PowerPoint 前同时检查 ROT 活动对象与 `POWERPNT.EXE` 进程；无法证明进程归属时宁可不预览，也不附着用户或孤儿会话。
- 退出隐藏实例采用 PID 归属、演示文稿数为 0、无可见窗口三重门禁；有任何用户文档或可见 PowerPoint 窗口时绝不退出。
- 渲染器登记并关闭全部自有快照引用，不再只关闭最后一份；历史版本首图完成后销毁一次性渲染会话，设置页 COM 检测也不再启动隐藏 PowerPoint。
- COM apartment 增加所有权计数，只反初始化渲染器自己建立的 COM 状态，避免干扰 Qt 剪贴板及其他组件。

### 全盘覆盖

- 修复扫描器按目录名整棵排除 `AppData`，导致公司软件存放在 `AppData\Roaming` 下的既有 PPT 永远无法建库的问题；`AppData`、`Roaming`、`LocalLow` 现纳入扫描和实时监听。
- PPT Doctor 自身 `%LOCALAPPDATA%\pptx-finder` 继续按规范化绝对路径硬排除，避免索引、版本对象和渲染快照被再次索引或留版。
- 增加扫描策略版本：旧索引升级后自动执行一次补漏，之后每 7 天在启动空闲窗口完整枚举固定磁盘；自动扫描固定单 worker，搜索或文件操作繁忙时延期。
- 无文件变化的覆盖扫描跳过全库版本聚类和 SQLite 维护，保留完整枚举能力而不制造无意义 CPU/IO。

### 预览等待

- 邻页预取从两页收敛为翻页方向的一页，避免不可中断的第二个 COM 导出阻塞用户翻页；预取空闲窗从 120ms 缩短为 80ms。
- 低清或旧缓存先立即显示，用户停留 160ms 后才补目标清晰度；连续翻页只保留最终停留页的高清请求。
- 缓存图已经达到当前预览窗口清晰度时直接作为最终图使用，不再进行一次冗余 PowerPoint COM 渲染。

### 结果卡与胶片报告

- 搜索结果卡改为纯文本布局，移除封面占位图、可见区滚动定时器、缩略图 worker、请求优先队列和 256 张内存缓存；搜索、排序、滚动、展开版本组不再触发任何结果图片工作。
- 图片资源只服务于用户实际选中的文件：右侧大图、命中页导航和最多一张相邻页预取；完成的大图不再复制回结果卡。
- 胶片报告新增“创作足迹”：活跃修改日、最长连续活跃天数、最忙月份和当前库修改时间跨度，明确按文件最后修改时间统计。
- 胶片报告新增“胶片 DNA”：平均页数、每页文字密度、短篇/长篇分布、近似内容复用家族和正文索引就绪率。
- 本年/本月/本周范围先在 SQLite 过滤文件，再只聚合范围内页文字；同一报告浮层内已生成的范围直接复用，切回不重复扫库。

## v1.0.8 — 2026-07-13

本版继续收口搜索语义与 PPT 预览等待：多词保留高召回，但把用户输入的完整短语放到相关度最前；翻页则用严格受限的现有会话预取换取缓存命中，不新增 PowerPoint 后台启动和并发渲染。

### 搜索

- 未加引号的多词查询（如 `AI SP`）仍按同页 AND 召回，但连续完整短语获得更高相关度；引号短语沿用严格短语语义。
- 相关度分为文件名完整短语、内容完整短语、去分隔符全字、部分匹配；`AI / SP` 可作为去分隔符全字命中，`AI SPARK` 不再冒充 `AI SP`。
- 新增“大小写”开关并置于排序控件旁，默认关闭；开启后使用原始文件名与页原文做大小写敏感验证，无需重建索引。
- 大小写敏感模式不显示无法原文复核的历史 FTS 提示，避免混入语义不一致的旧版本命中。

### 预览与降载

- 修复原相邻页预取被本工具自身隐藏 PowerPoint 会话误判为“用户正在使用”而长期跳过的问题。
- 后台预取只能复用当前用户预览已经打开的同一安全快照；没有现成会话就直接跳过，不启动/附着 PowerPoint，不打开另一份演示文稿。
- 预取保持单 COM 串行，空闲等待由 450 ms 收敛到 120 ms；最多缓存当前方向相邻页与下一命中页，新预览会清空尚未开始的旧预取。
- 右侧高清命中页完成后直接缩小复用为当前结果卡缩略图，减少占位等待且不增加 COM 工作。

## v1.0.7 — 2026-07-13

本版修复 v1.0.6 发布后在生产机空闲采样中发现的启动高 CPU：旧索引超过 24 小时时，程序会立即全盘遍历固定磁盘，连续占满一个 CPU 核，并与搜索、排序和预览争抢资源。

- 非空索引启动时不再自动全盘扫描；旧索引立即可搜，彻底移除周期性全盘遍历造成的启动高负载。
- 每 24 小时只在后台 `stat` 已索引路径，并轻量枚举这些文件的直接父目录；核对修改、待处理状态及同目录新文件，不递归未知目录、不解析未变化文件。
- 普通离线变化仍交给串行 `LiveIndexer` 增量处理，一批最多 200 个并跨启动轮转；首次建库中断留下的 `pending` 不受该上限影响，在当前会话每 1.5 秒续建一个，避免既漏内容又重新形成 CPU/IO 风暴。
- 运行期间的新建/保存/删除继续由 watcher 实时捕获；关闭期间同一已索引目录的新文件会在下次轻量核对发现。全新目录与离线删除需用户主动“重新扫描全盘”，避免因拔盘或云盘暂离而误删索引。

## v1.0.6 — 2026-07-13

本版集中解决搜索、排序与预览争抢主线程造成的卡顿，并收紧 PowerPoint 安全边界。

### 搜索与排序

- 原文验证改为随 FTS 结果一次联表读取，消除逐命中页查询；生产库高频短词冷查约 0.23 秒、热查约 0.10 秒。
- 相关度使用硬分层：文件名完全匹配 > 内容完整匹配 > 部分匹配；结果卡明确标注完全命中类型。
- 新增主排序 + 次排序组合，例如“文件名 + 最近修改”；重复条件自动去重。
- 搜索结束立即收口状态，不再等待索引统计；被新输入取消的 FTS 查询向工作线程传播，不返回半截结果。
- watcher 文件风暴触发的搜索改为 2.5 秒空闲合并，避免刚完成就被同词重复搜索中断。

### 界面与后台降载

- 大结果集只创建首屏 12 张卡片，其余滚动或点击后按需加载；200 条结果首屏构建由约 711 ms 降至约 70 ms。
- 排序、筛选和换肤不再反复创建全部结果卡；200 条结果切换排序实测约 31 ms。
- 跨版本提示延后到主搜索稳定后执行；历史提示、统计读取和自动预览不再同时抢搜索收口。
- 版本窗口 4000+ 文档筛选增加 180 ms 输入合并与等高列表优化。
- 托盘隐藏时停止 UI 卡顿采样与可见缩略图定时器；取消启动时自动拉起 PowerPoint 预热。

### 预览与安全

- 交互预览与相邻页预取复用同一安全快照，避免原文件/快照来回打开；后台最多预取一页，并等待 450 ms 交互空闲窗口。
- 预取分辨率与实际预览一致，避免同一页先导出 960px、再重复导出 1280px。
- 渲染 IPC 超时由 75 秒降至 20 秒；超时不再原样重试，最坏等待从两轮约 150 秒收敛为单轮。
- 彻底移除任何调用 `PowerPoint.Application.Quit()` 的路径：部分 Office 环境下 `DispatchEx` 仍可能返回用户现有实例，只允许关闭本工具自己打开的安全快照并释放引用。

## v1.0.5 — 2026-07-12

本版集中强化“PPT 版 git”的可靠性、可诊断性和长期运行效率。

### 可靠性

- 保存只从同一份稳定临时副本完成解析、哈希和入库，规避 PowerPoint 原子保存竞态。
- 无效/截断 PPTX 不再写成恢复点；watcher 会按退避节奏补拍最终稳定版本。
- 去重快照入库前同时验证“可解析 + 规范内容哈希一致”，对象静默漂移时自动回退为完整快照。
- 恢复与导出改为同目录临时重组、PPTX 解析、规范内容哈希比对、原子替换四道门禁。
- 恢复前自动留当前版时会保护用户已选中的旧恢复点，避免恰逢配额上限时先清理、后恢复失败。
- 删除事件实时同步版本状态；离线巡检按持久游标轮转，不再长期饿死排序靠后的文档。
- 同路径离线重建会重新激活原文档；找回会跳过坏的最新版并退到最近健康版本。

### 完整性与维护

- 新增版本健康字段、快速仓库检查与用户可触发的全对象深度哈希检查。
- 损坏对象会映射到全部关联恢复点并隔离；浅检不会误清除深检隔离状态。
- 隔离版本不能恢复、导出、预览、从历史搜索入口操作，也不能作为复制文件的分支基线。
- “已留版 / 可回退”KPI 只统计健康恢复点；总版本与隔离版本保持独立物理口径。
- 清单与数据库规范内容哈希互校，发现元数据漂移时同样隔离。
- 手动深检与对象迁移/GC 使用独立可重入协调锁，避免迁移窗口造成假隔离，同时不阻塞保存快照。
- 重型对象迁移与 GC 改为七天节流；GC 保留分支恢复根，并在删除前验证完整引用图。
- 生产规模快速审计消除重复对象路径探测，实测由 109 秒降至约 2–3 秒。

### 体验

- 版本窗口新增按文件名/路径筛选，以及现存/已删除范围切换。
- 时间线明确标识并禁用无效恢复点；跨版本搜索沿用同一健康门禁。
- 设置页新增 50 / 100 / 200 / 不限保留上限与“深度检查版本库”。
- 默认保留 100 版，并优先保留跨编辑会话里程碑，避免一次密集保存挤掉长期历史。
- 隔离版本保留待修复但不占健康版本配额，不会挤掉可用恢复点。
- 开机自启快捷方式每次启动自愈到当前版本目录，修复升级后仍指向已删除旧 EXE 的问题。
