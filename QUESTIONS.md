# 累积问题（明早向用户汇报）

> 自主夜班期间遇到的、需要用户拍板或知悉的点。不阻塞开发，先按合理默认推进。

## 待确认的默认决策
1. **扫描排除目录**默认包含名为 `temp/tmp/cache/appdata` 等的文件夹——可能误排放在这些目录里的真实 PPT。倾向保留（命中率低、收益高），请确认。
2. **超大文件阈值**：>200MB 的 PPT 跳过内容解析（仍可文件名命中）。是否合适。
3. **全局热键**默认 `Ctrl+Alt+P`——是否与你常用软件冲突，需要改。
4. **繁简通搜**列为 P1，今晚若时间紧可能不含 opencc，先做基础归一（全角/大小写）。

## 技术风险/观察
（开发中补充）
